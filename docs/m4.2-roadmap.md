# M4.2 路线图：从「活的 NPC」到「活的小镇」

| 文档版本 | 日期 | 说明 |
|---|---|---|
| 0.1 | 2026-04-20 | 初稿：承接 M4.1 真 LLM 关盘，把业主已选的 5 条升级路径（M4.1 尾补丁 / 观测性 / 记忆向量化 / 反思循环 / 事件总线）串成可顺序执行的子里程碑 M4.2.0 ~ M4.2.4；**仅规划，不动代码**，待评审 |
| 0.2 | 2026-04-20 | **首轮评审结论录入**：①执行顺序保持 0→1→2→3→4；②**记忆存储切换 Qdrant**（放弃 MySQL 客户端余弦，直接上成熟向量库）；③环境变量默认值先用当前值；④PR 批次保持 3 批；⑤§1.2 非范围补增 3 条确认排除项；⑥无新增功能需求 |
| 0.3 | 2026-04-20 | **✅ M4.2.0 完成**：`ai_config.budget_tokens_per_tick` 列上线；scheduler `meta_warns` 滚动窗口 + `X-Meta-Warn` 响应头；tick 前预算硬检查（超支写 skipped）；前端 NpcForm 字节计 + 软/硬阈值警示 + 硬阈值提交拦截；Sandbox 顶栏 meta-warn 徽章与去重 toast；新增 4 条单测，后端 34/34 全绿，前端 vue-tsc build 通过。下一节点 **M4.2.1 观测性** |
| 0.4 | 2026-04-21 | **M4.2.1 细设评审通过**（见 [`engine-integration-m4.2.1.md`](./engine-integration-m4.2.1.md) v0.2）：Tokenizer = `tiktoken` (wasm)；时间线 = P1 右侧独立列；WS = `ws` + express 同端口；单价表硬编码；按 **a(后端计费) / b(WS) / c(前端)** 3 个 commit 拆分。§12「明确不做」4 条降级为「后续扩展点」入档，不进本节点。进入 **M4.2.1.a 开发** |
| 0.5 | 2026-04-21 | **✅ M4.2.1.a 后端计费完成**（细设 v0.3）：`ai_call_log` 扩 4 列 + 复合索引；`engine/tokenCounter.ts`（tiktoken + 硬编码单价表 + `COST_ACCOUNTING_ENABLED` 开关）；`llmClient` usage/estimate 双路径 + `onMetrics` 回调；`graph/build.ts` tick 粒度聚合；`scheduler.lastTickTokensByNpc` 真实化，M4.2.0 budget skip **真正生效**；后端 **41/41** + tsc build 通过；新增 11 条单测。下一步 **M4.2.1.b WebSocket** |
| 0.6 | 2026-04-21 | **✅ M4.2.1.b WebSocket 完成**：`ws` 依赖；`engine/wsServer.ts` 挂载 `/ws/engine`（express 同端口复用），自定义关闭码 4000/4001/4002 + 心跳 + `OBSERVABILITY_WS_MAX_PER_SCENE`；`TickEvent` 扩展（`tick.npc.updated` 加 `status/duration/tokens/cost_usd`、新增 `meta.warn`、`tick.end` 加 `cost_usd_total`）；`scheduler.pushMetaWarn` 同步 emit；`GET /api/engine/status` 响应附加 `ws_endpoint`；前端 `openEngineWs`（指数退避 1/2/4/8/16s + idle-timeout 重连 + degraded 回落 3s 轮询）；`Sandbox.vue` WS 优先订阅 + WS 状态徽章 + `meta.warn` 实时推；后端 **47/47**（新增 6 条 WS 单测）+ tsc build 通过，前端 vue-tsc 通过。下一步 **M4.2.1.c 前端时间线** |
| 0.7 | 2026-04-21 | **✅ M4.2.1.c 前端时间线完成 → M4.2.1 整节点收官**：新增 `types/timeline.ts` 与 `SandboxTimeline.vue`（P1 右侧独立列 panel / 小屏 <1280px 自动切 el-drawer），内部 20 条 ring buffer + 每行可展开 NPC 明细（✓/⧗/✕ 状态、duration、prompt→completion tokens、cost、`meta_summary.latest_say/action`）；`Sandbox.vue` 订阅 WS 三种事件聚合 timeline 状态 + 顶栏新增 `Σ $0.xxxx · Ntok` 会话累计标签（点击切面板/抽屉），切场景或组件卸载归零；窗口 resize 监听动态切 panel/drawer。前端 vue-tsc 通过、vitest 13/13 绿，后端零变更仍 47/47。**下一节点 M4.2.2 记忆向量化** |
| 0.15 | 2026-04-21 | **✅ M4.2.4.b Engine Node + REST API + Scheduler 集成完成 + live smoke 全绿**：①全 ⭐ 推荐路径拉票定稿（6 题）：event-intake 节点成功后立即写 consumed（Q1a，失败不写等下 tick 重试）、scene 级一次 DB 查询 + 内存分发（Q2b，tick 内从 N² 降到 1 次 SQL）、POST 返回完整 row + 同步 WS emit（Q3c）、WS 事件命名 `scene.event.created`（Q4a）、event-intake 失败降级为 empty 不阻 plan（Q5b）、visible_npcs 应用层过滤（Q6b）。②数据查询层：`engine/event/fetchRecentEvents.ts`（`fetchRecentSceneEvents` 场景级 DESC 查询 + hardLimit 默认 500 防雪崩 / `fetchConsumedSet` 批量 (events×npcs) 预读 + 空输入短路 / `writeConsumedBatch` `INSERT IGNORE` 幂等 + `UPDATE scene_event SET consumed_tick` 首次消费标记，payload/visible_npcs 非法值降级 null）；`engine/event/intake.ts::pickEventsForNpc` 纯函数（5 分支：empty/injected/visible_npcs 过滤/consumed 去重/maxPerTick 截断，`dropped_count` 全程累计）。③Scheduler 集成：tick 顶部 `if eventCfg.enabled` 预取一次事件 + 消费集，failure 降级空集并 `console.warn`；per-NPC task 内调用 `pickEventsForNpc` + `buildEventBlock` 注入 `runGraph.eventBlock`；`GraphInput` 新增 `eventBlock?` 字段透传到 `buildPlanPrompt`；NPC 成功 `persistNpcTick` 后立即 `writeConsumedBatch`（失败不阻，仅 warn）。④REST API：`controllers/sceneEvents.ts` 三接口（`POST /api/scene/:id/events` zod 校验 + scene 存在性 + insert + 回查 row + bus.emit `scene.event.created` / `GET /api/scene/:id/events?limit=50&since=<id>` 支持增量轮询 + 默认/上限分页 / `DELETE /api/scene/:id/events/:eid` 幂等 + FK 级联清 consumed），错误码对齐 M4.2.3.c 风格（`INVALID_PARAM/INVALID_BODY/SCENE_NOT_FOUND/EVENT_NOT_FOUND/INTERNAL`）；路由 `routes/scene.ts` 注册子路由（在 `/:id` 通配之前）；⑤`TickEvent` 联合扩展 `scene.event.created`，`wsServer.serializeEvent` 默认分支自动转发。⑥26 条新单测（pickEventsForNpc 5 分支 + actor 透传 7、fetchRecentSceneEvents SQL 参数化 + normalize 2、fetchConsumedSet 2、writeConsumedBatch 2、POST controller 6、GET controller 4、DELETE controller 3），后端 **152/152** + tsc build + Live smoke（POST 合法/空 content/不存在 scene → GET list → DELETE → GET since 增量）全绿。下一步 **M4.2.4.c 前端事件注入面板 + WS ring buffer**。 |
| 0.14 | 2026-04-21 | **✅ M4.2.4.a 事件总线数据层 + Prompt 基建完成**：①全 ⭐ 推荐路径拉票定稿（Q1~Q8）：per-NPC 可见性过滤（Q1a）、per-(event×npc) 精确去重表（Q2b）、4 枚举类型锁定（Q3a）、actor 自由字符串（Q4a）、plan.user 头部注入（Q5a）、WS `scene.event.created`（Q6a，留 b 落地）、前端 2 预设 + 💬自定义（Q7b）、event-intake 内部降级不阻 tick（Q8a）。②数据层落地：`scene_event` 主表（8 列 + 2 索引，`type` 强 ENUM + `visible_npcs/payload` JSON + `consumed_tick` 调试列）+ `scene_event_consumed` 去重表（`PRIMARY KEY(event_id, npc_id)` + 2 索引 + 双外键级联）+ `migrate-m424.ts` 幂等迁移脚本 + `package.json` 登记 `db:migrate:m424`。③env：`.env.example` / `.env` 新增 `EVENT_BUS_ENABLED=true` + `EVENT_LOOKBACK_SECONDS=60` + `EVENT_MAX_PER_TICK=10`，独立 `engine/event/config.ts` 模块（`getEventConfig()` + `resetEventConfig()`，避免污染 memory config）。④类型 & Prompt：`engine/event/types.ts`（`EVENT_TYPES` 常量 + `SceneEventRow/CreateSceneEventInput/EventBlockItem/EventIntakeResult`）、`engine/event/prompts.ts`（`createSceneEventSchema` zod 强校验 5 字段边界 + `hasEvents/buildEventBlock` 格式化 + 单条 160 字截断带省略号）；`graph/prompts.ts::buildPlanPrompt` 新增 `eventBlock?` 注入 user 消息最前（事件 > 场景 > 同场角色 > 记忆 > tick），空字符串与不传向后兼容。⑤27 条新单测（config env 解析 7 + EVENT_TYPES 稳定性 1 + zod 边界 11 + hasEvents/buildEventBlock 5 + buildPlanPrompt 注入 3），后端 **126/126** + tsc build + migration 幂等复跑通过。下一步 **M4.2.4.b Engine Node + REST API**。 |
| 0.13 | 2026-04-21 | **✅ M4.2.3.c 前端展示 + 手动触发 API 完成 + live smoke 全绿 → M4.2.3 整节点收官**：①全 ⭐ 推荐路径：reflect.ts 新增 `force?: boolean`（跳 dryRun/everyN/tick 周期三判定，保留「空记忆 → skipped」兜底）；②新增 `controllers/reflection.ts::reflectOnce`（`scene_npc` 绑定校验 + `ai_config_id` 为空 → `NPC_AI_CONFIG_MISSING`、status!=1 → `AI_CONFIG_INVALID`；tick 取值：运行中用 `scheduler.status().tick`，停机用 `MAX(npc_tick_log.tick)+1`；读 `simulation_meta.memory_summary` 作 prevSummary；`status='generated'` 时 `bus.emitEvent({type:'reflection.created'})` 与周期触发同链路）；路由 `POST /api/engine/reflect`；③10 条控制器单测（参数校验 2 + 绑定/配置错误 3 + tick 取值 3 + 返回+WS 2）；④前端 `types/reflection.ts`（`REFLECTION_THEMES`/`ReflectionItem`/`WsReflectionCreatedMsg`/`ReflectionRingEntry`/`THEME_LABELS`）+ `engine.ts` WsEngineMsg 扩展；`api/engine.reflectOnce` + `EngineWsHandlers.onReflection`；`components/SandboxReflections.vue` 抽屉（npc×tick 分组卡片、3 theme 定序展示、溯源 memory_id 展开、清空按钮）；`Sandbox.vue` ring buffer（20 条 + 按 `reflection_ids[0]` 去重）+ 顶栏 🧘 徽章 + WS handler 接入 + 右键菜单「手动触发反思」（toast loading 25~40s，成功自动打开抽屉）+ 切场景清空；后端 **99/99** + tsc build，前端 vue-tsc + vite build 通过。⑤**live smoke PASS**（`curl POST /api/engine/reflect scene=1 npc=1`）：10.3s 返回 `status=generated` + 3 条主题完整；`npc_reflection` 新增 #7/#8/#9 三行（memory_id 回填 100%、source=13 条真实引用）；参数校验/NPC 未在场景双反例返回码正确。**M4.2.3 反思循环** 整节点完成，下一节点 **M4.2.4 事件总线 / 跨节点交互**。 |
| 0.12 | 2026-04-21 | **✅ M4.2.3.b Engine Node 完成 + live smoke 全绿**：①新增 `engine/reflection/reflect.ts` `reflectIfTriggered()`：触发判定（`tick % everyN === 0 && !dryRun && everyN>0`）→ 拉最近 K 条 memory（按 `created_at DESC`，空则 skip 避免瞎编）→ `chatCompletion` 2 次重试 + zod + `assertThemesComplete` → INSERT 3 条 `npc_reflection`（单 INSERT 多 VALUES 保证 id 连续）→ 每条反思再 `storeMemory(type='reflection', importance=8)` 反哺 `npc_memory`+Qdrant → UPDATE `memory_id` 回填双向索引；失败矩阵全吞（节点内降级，主流程不抛）。②`graph/build.ts` 把 reflect 串到 memory-summary 之后 + `GraphOutput.reflection` 扩展 + `simulation_meta.debug.reflection`（status/ids/source_ids）。③`engine/types.ts` 新增 `reflection.created` TickEvent；`scheduler.ts` 仅在 `status==='generated'` 时 emit；`wsServer.serializeEvent` fallthrough 自动透传（0 改动）。④10 条 reflect 单测（触发/dryRun/周期不命中/everyN=0/空 memory/LLM 非 JSON/zod 失败/主题不完备/INSERT 失败/反哺失败但 generated 保全），后端 **89/89** + tsc build。⑤**live smoke PASS**（`PORT=3001` 临时实例 × 6 tick × 2 NPC × 通义千问）：`tick_log` 12/12 success + 4 budget-skip；`npc_reflection` 精确 6 行（2 NPC × 3 theme 于 tick=5 命中），`memory_id` 回填 100%、`source_memory_ids` 含 10~14 条真实引用；Qdrant `npc_memory` collection 新增 6 个 `type=reflection, importance=8` 点（id 25~30），与 MySQL 反射 id 完美闭环。⑥新探针 `scripts/debug/probe-reflections-data.ts`（总数 + 回填率 + 按 (npc,tick) 分布 + 最新 9 条预览）。下一步 **M4.2.3.c 前端展示 + 手动触发 API**。 |
| 0.11 | 2026-04-21 | **✅ M4.2.3.a 反思数据层 + Prompt 基建完成**：①全接受拉票推荐（Q1~Q7 全 a）：反思固定 3 条 theme（goal/emotion/relation）、触发 `tick % REFLECT_EVERY_N_TICK === 0`、自动反哺 `simulation_meta.plan`、最近 20 条 memory 按时间倒序、反思入 npc_memory+Qdrant（`type=reflection, importance=8`）、拆 3 batch、手动 API 随 .c 交付、默认推 WS `reflection.created`。②数据层落地：新建 `npc_reflection` 表（7 列 + 4 索引，含 `source_memory_ids JSON` 溯源列与 `memory_id BIGINT` 反向双写列）+ `migrate-m423.ts` 幂等迁移脚本 + `package.json` 登记 `db:migrate:m423`。③类型 & Prompt：`engine/reflection/types.ts`（`REFLECTION_THEMES` 常量 + `ReflectionItem/Result`）、`engine/reflection/prompts.ts`（zod schema 强约束 3 条 items + content 200 字截断、`assertThemesComplete` 业务级去重 + 完备性、`buildReflectionPrompt` 复用 `SYSTEM_PREFIX`）。④env：`REFLECT_EVERY_N_TICK=5` + `REFLECT_RECENT_MEMORY_K=20` 接入 `memory/config.ts`（合并到 `MemoryConfig.reflection` 子对象，0 = 关闭自动反思）。⑤12 条新单测（zod 边界 6 + 主题完备性 2 + 格式化 2 + prompt 构造 2），后端 **79/79** + tsc build + migration 幂等通过。下一步 **M4.2.3.b Engine Node 实现**。 |
| 0.10 | 2026-04-21 | **🟢 M4.2.2 live LLM smoke PASS → 整节点收官**：`scripts/smoke-engine.ts` 打通义（2 ticks × 2 NPC，chat+embed 均 id=4）；`tick_log` 4/4 全 success、`ai_call_log` 12/12（plan=4/speak=4/memory=4）、`npc_memory` 新增 8 行全 `embed_status=embedded`（每 tick × NPC 写 observation + dialogue 两条）、Qdrant collection `npc_memory` 首次 upsert 自动创建（status=green, points_count=8, size=1536, Cosine）。顺手修 **`/api/ai-logs` HTTP 500**（mysql2 prepared 对 LIMIT/OFFSET placeholder 不稳，改为已 sanitize 的整数内联）。新增两枚只读运维探针：`scripts/debug/probe-embed-dim.ts`（真调 DashScope 确认维度=1536 + 凭据有效）、`scripts/debug/probe-memory-state.ts`（npc_memory 分布 + Qdrant collection 快照）。进入 **M4.2.3 反思循环** 的起跑线。 |
| 0.9 | 2026-04-21 | **✅ M4.2.2.c 前端降级徽章 + embedding 指针式注入完成**：①后端 `engine/memory/config.ts` 新增 `embedAiConfigId` + env `MEMORY_EMBED_AI_CONFIG_ID` 开关（0=禁用走 chat aiCfg，正整数=ai_config 表主键）；②新增 `engine/memory/embedAiConfig.ts`（60s TTL 缓存 resolver；status!=1 / api_key 空 / 记录缺失统一回落 chat aiCfg，不抛错不阻塞 tick）；③`retrieve.ts` / `store.ts` 改用 `resolveEmbedAiConfig()`，embedText options 传 embed model override；④前端 `types/engine.ts` 扩 `memory_degraded?: boolean`；⑤`Sandbox.vue` 顶栏 WS 徽章之后、Σ 徽章之前插 🧠 记忆降级徽章，仅 `engineStatus.memory_degraded === true` 时可见，`.memory-warn-pill` 复用 `meta-warn-flash` keyframes 走 infinite alternate；⑥`.env.example` / `.env` 新增 `MEMORY_EMBED_AI_CONFIG_ID` + 完整 memory/qdrant/embed_cache 中文注释；⑦调试脚本 `_probe-ai-config.ts` 归档为 `scripts/debug/probe-ai-config.ts` 并补文档块。后端 **67/67** + tsc build 通过，前端 vue-tsc + vite build 通过。下一步 **live LLM smoke**（验证通义千问 embedding 真写入 Qdrant） |
| 0.8 | 2026-04-21 | **✅ M4.2.2.a 数据层完成**（补录，见 commit `223d19c`）：`npc_memory` MySQL 表（元数据 + 原文 + `embed_status`）；`engine/memory/config.ts`（全局 env 统一读取 + zod 校验）；`engine/memory/qdrantClient.ts`（单例 QdrantMemoryStore，`ensureCollection` / `upsert` / `search` / `deleteByIds` / `health`，3s timeout + 1 次重试 + `QdrantUnavailableError` 供外层降级）；`utils/llmClient.embedText` 扩展（复用 ai_config，磁盘 LRU + 内存缓存、SHA1 键、30 天 TTL）；新增 5 条缓存单测 + 3 条 config 单测。**✅ M4.2.2.b Engine Node 完成**：新增 `engine/memory/retrieve.ts`（prevSummary + neighbors query → embed → Qdrant search → MySQL 反查 → 按相似度重排 + 异步 access 统计）+ `store.ts`（ruleBasedImportance 规则打分 → MySQL INSERT(`pending`) → embedText → Qdrant upsert → UPDATE `embedded`；embed 失败 `failed`，upsert 失败 `pending` 保留向量）+ `types.ts`（MemoryEntry / RetrieveResult / StoreResult）；`graph/prompts.ts` 新增 `buildMemoryBlock`，`buildPlanPrompt` / `buildSpeakPrompt` 支持 `memoryBlock` 注入；`graph/build.ts` 按 **retrieve → plan → speak → store(say/action) → memory-summary** 串联 + 三级降级（Qdrant→MySQL importance→空）；`scheduler.memoryDegradedAt` + `status().memory_degraded`（5min 新鲜窗口）；`EngineStatus.memory_degraded?: boolean` 扩展；新增 11 条单测（retrieve 4 / store 4 / importance 3），后端 **67/67** + tsc build 通过。下一步 **M4.2.2.c 前端顶栏 🧠 徽章 + live smoke**（接 OpenAI embedding provider） |

> 关联：[`engine-selection.md`](./engine-selection.md) | [`engine-integration-m4.1.md`](./engine-integration-m4.1.md) v1.0 | [`requirements-character-scene.md`](./requirements-character-scene.md)

---

## 0. 摘要（TL;DR）

| 目标维度 | M4.1 已交付 | M4.2 要补齐 |
|---|---|---|
| **NPC 思考** | plan → speak → memory 线性图（zod） | **记忆向量检索** + **反思抽象**（目标/情绪/关系） |
| **世界感知** | 只感知到同场景 `neighbors` 名字 | **场景事件**（下雨/打斗/节日）可编程注入 |
| **观察成本** | 后端日志 + 3s 轮询 + cost_usd_total=0 占位 | **WebSocket 推送** + **真实 token/cost 统计** + 前端时间线 |
| **工程债** | `X-Meta-Warn` / budget / meta 前端校验未做 | **0.5 天清零**，不留尾巴 |

**总工作量 ≈ 12.5 天**（单人；含联调与最小测试，不含代码评审 buffer）。

**架构原则**：每个子里程碑都是一次**独立可回滚的纵切**（数据层 → API → 引擎 → 前端 → 测试 → 文档），失败可单独撤销。继续不引入 LangGraph.js —— 保留 M4.3 重构窗口。

---

## 1. 范围

### 1.1 范围内（M4.2）

1. **M4.2.0 尾补丁**：HTTP `X-Meta-Warn` 头、前端 meta 软校验提示、`ai_config.budget_tokens_per_tick` 字段与校验。
2. **M4.2.1 观测性**：`/ws/engine` WebSocket 事件推送；`ai_call_log` 记录真实 tokens/cost；前端 tick 时间线浮窗。
3. **M4.2.2 记忆向量化 M-1→M-2**：`npc_memory` 新表 + embeddings 检索；plan/speak 节点注入 top-K 相关记忆；`memory_summary` 保留向下兼容。
4. **M4.2.3 反思循环**：每 N tick 触发 reflect 节点；`npc_reflection` 表归档高层抽象；`simulation_meta.plan` 提升稳定性。
5. **M4.2.4 事件总线**：`scene_event` 表 + `POST /api/scene/:id/events` 注入 API；tick 头自动拉事件注入 agent 输入；前端沙盒事件面板（下雨/加 NPC/触发剧情）。

### 1.2 非范围（M4.3+ 再考虑）

**已评审确认的排除项**：

- LangGraph.js / LangGraph Python 接管推理图（M4.3）
- Human-in-the-loop（玩家插话、打断、角色扮演）（M4.3）
- NPC 动作副作用对世界状态的真实变更（NPC 说"我去买酒"只是对话记录，不改库存/坐标/关系图）（M4.4）
- NPC 间直接对话管线 / turn-taking（目前 `neighbors` 只提供名字列表，不做点对点对话路由）（M4.3）
- 场景级时间系统（昼夜 / 季节 / 事件调度器）（M4.4）
- 跨场景联动 / 多场景同进程并跑（M4.4）
- 空间网格寻路 / NPC 物理移动（M5 沙盒游戏化）
- 语音/TTS / Avatar 形象生成（M5+）

---

## 2. 依赖与环境变量

### 2.1 新增依赖

| 依赖 | 用途 | 子里程碑 |
|---|---|---|
| `ws`（Node） | WebSocket 服务端 | M4.2.1（锁定 W1） |
| `tiktoken`（wasm） | 本地 token 计数 | M4.2.1（锁定 B） |
| `@qdrant/js-client-rest` | **Qdrant 向量库客户端**（记忆检索主存储） | M4.2.2 |
| 复用 `llmClient.embedText` | 生成向量 | M4.2.2 |

**Qdrant 部署方式**：本地开发用官方 Docker 镜像（`docker run -p 6333:6333 qdrant/qdrant`），生产可切 Qdrant Cloud 或自建集群。单节点即可胜任本项目预期规模（NPC × 1w 级记忆）。

前端无新依赖。

### 2.2 新增环境变量（backend/.env）

```env
# M4.2.1 观测性
OBSERVABILITY_WS_ENABLED=true        # /ws/engine 挂载；false 时前端自动回落 3s 轮询
OBSERVABILITY_WS_MAX_PER_SCENE=10    # 单场景并发订阅上限；超出按最老先踢（4001）
COST_ACCOUNTING_ENABLED=true         # 统计 tokens × 单价

# M4.2.2 记忆向量化
MEMORY_EMBED_ENABLED=true
MEMORY_EMBED_MODEL=text-embedding-3-small  # 复用 ai_config provider
MEMORY_TOP_K=5                  # plan/speak 注入的相关记忆条数
MEMORY_RETENTION_DAYS=30        # 超期记忆归档/降权
# --- Qdrant 向量库 ---
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=                 # 本地可留空；Qdrant Cloud 必填
QDRANT_COLLECTION=npc_memory    # 全局共享一个 collection，用 payload.npc_id 分区
QDRANT_VECTOR_SIZE=1536         # 对齐 text-embedding-3-small

# M4.2.3 反思
REFLECT_EVERY_N_TICK=5          # 每 5 tick 触发一次
REFLECT_RECENT_MEMORY_K=20      # 拉取最近 K 条记忆做反思

# M4.2.4 事件
EVENT_LOOKBACK_SECONDS=60       # tick 头读近 60s 内未消费事件
EVENT_MAX_PER_TICK=10           # 单 tick 最多感知事件数
```

---

## 3. 数据层变更

### 3.1 新表（M4.2.2）—— 双存储：MySQL 元数据 + Qdrant 向量

**选型决策**：放弃 MySQL 客户端余弦方案，直接上 **Qdrant** 作为向量主存储。理由：①单节点 Docker 启动成本极低；②原生近邻检索与过滤器表达式（`npc_id in [...]` / `importance >= 5`）；③后续加 payload-based 过滤（type=reflection、scene_id 等）零成本；④避免 10k+ 规模的二次迁移。

**MySQL 侧只存元数据 + 冗余原文**，作为 Qdrant 的真实来源备份 & 后台管理界面数据源：

```sql
-- M4.2.2 NPC 长期记忆元数据（真实向量在 Qdrant）
CREATE TABLE IF NOT EXISTS npc_memory (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID（等同 Qdrant point id）',
  npc_id BIGINT NOT NULL COMMENT 'NPC ID',
  scene_id BIGINT DEFAULT NULL COMMENT '来源场景（可为空，表示全局）',
  tick BIGINT DEFAULT NULL COMMENT '来源 tick',
  type VARCHAR(16) NOT NULL COMMENT 'observation/dialogue/reflection/event',
  content TEXT NOT NULL COMMENT '记忆原文（≤ 1000 字）',
  importance TINYINT DEFAULT 5 COMMENT '1~10 重要度（LLM 或规则打分）',
  embed_status VARCHAR(16) DEFAULT 'pending' COMMENT 'pending/embedded/failed，失败可异步重嵌',
  embed_model VARCHAR(64) DEFAULT NULL COMMENT '实际使用的嵌入模型',
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  last_accessed_at DATETIME(3) DEFAULT NULL COMMENT '上次被检索到的时间',
  access_count INT DEFAULT 0 COMMENT '被检索次数',
  INDEX idx_npc_time (npc_id, created_at),
  INDEX idx_npc_importance (npc_id, importance DESC),
  INDEX idx_embed_status (embed_status),
  CONSTRAINT fk_mem_npc FOREIGN KEY (npc_id) REFERENCES npc(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='NPC 长期记忆元数据';
```

**Qdrant collection schema**（应用启动时确保创建）：

```jsonc
{
  "collection": "npc_memory",
  "vectors": { "size": 1536, "distance": "Cosine" },
  "payload_schema": {
    "npc_id":   { "type": "integer", "index": true },
    "scene_id": { "type": "integer", "index": true },
    "type":     { "type": "keyword", "index": true },
    "importance": { "type": "integer", "index": true },
    "tick":     { "type": "integer" },
    "created_at": { "type": "integer" }  // unix ms
  }
}
```

**写入契约**：MySQL INSERT 先落，拿到 `id` → Qdrant `upsert(point_id=id, vector, payload)`。任一失败走后台重试队列（`embed_status=failed` 标记，cron 扫描重嵌）。**MySQL 是真实来源**，Qdrant 可视为派生索引。

**删除契约**：`DELETE FROM npc_memory WHERE id=?` → `qdrant.delete([id])`；两步幂等，允许短暂不一致（孤儿 point）。

**检索契约**：
```typescript
await qdrant.search({
  collection: 'npc_memory',
  vector: queryEmbedding,
  filter: { must: [{ key: 'npc_id', match: { value: npcId } }] },
  limit: MEMORY_TOP_K,
  with_payload: true,
});
```
返回后用 payload 里的 `id` 反查 MySQL 拿完整 `content`（Qdrant payload 不冗余 content，避免数据双写同步问题）。

### 3.2 新表（M4.2.3）

```sql
-- M4.2.3 NPC 反思归档（高层抽象，区别于原始观察）
CREATE TABLE IF NOT EXISTS npc_reflection (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
  npc_id BIGINT NOT NULL COMMENT 'NPC ID',
  scene_id BIGINT NOT NULL COMMENT '场景ID',
  tick BIGINT NOT NULL COMMENT '触发反思的 tick',
  theme VARCHAR(64) DEFAULT NULL COMMENT '反思主题（目标/情绪/关系）',
  content TEXT NOT NULL COMMENT '反思产物',
  source_memory_ids JSON DEFAULT NULL COMMENT '参考的记忆 id 数组',
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  INDEX idx_npc_tick (npc_id, tick DESC),
  CONSTRAINT fk_ref_npc FOREIGN KEY (npc_id) REFERENCES npc(id) ON DELETE CASCADE,
  CONSTRAINT fk_ref_scene FOREIGN KEY (scene_id) REFERENCES scene(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='NPC 反思归档';
```

### 3.3 新表（M4.2.4）

```sql
-- M4.2.4 场景事件（外部注入 / NPC 产生）
CREATE TABLE IF NOT EXISTS scene_event (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '主键ID',
  scene_id BIGINT NOT NULL COMMENT '场景ID',
  type VARCHAR(32) NOT NULL COMMENT '事件类型：weather/dialogue/system/plot',
  actor VARCHAR(64) DEFAULT NULL COMMENT '事件发起者（NPC 名 / system / user）',
  content TEXT NOT NULL COMMENT '事件描述（给 LLM 看的自然语言）',
  payload JSON DEFAULT NULL COMMENT '结构化附加信息（可选）',
  visible_npcs JSON DEFAULT NULL COMMENT 'NULL=全场景可见，数组=指定 NPC 可见',
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  consumed_tick BIGINT DEFAULT NULL COMMENT '首次被 tick 消费时间（供去重）',
  INDEX idx_scene_time (scene_id, created_at DESC),
  CONSTRAINT fk_evt_scene FOREIGN KEY (scene_id) REFERENCES scene(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='场景事件流';
```

### 3.4 现有表追加列

| 表 | 列 | 默认 | 用途 | 子里程碑 |
|---|---|---|---|---|
| `ai_config` | `budget_tokens_per_tick INT DEFAULT 2000` | 2000 | 每 NPC 每 tick 总 token 预算上限 | M4.2.0 |
| `ai_call_log` | `prompt_tokens INT` / `completion_tokens INT` / `cost_usd DECIMAL(10,6)` | NULL/0 | 真实 token / cost 记账 | M4.2.1 |
| `npc_tick_log` | `reflection_id BIGINT DEFAULT NULL` | NULL | 关联本 tick 是否触发反思 | M4.2.3 |

> 全部幂等迁移脚本：`backend/src/db/migrate-m42.ts`，一键 `npm run db:migrate-m42`。  
> **Qdrant 初始化**：独立脚本 `backend/src/db/init-qdrant.ts`（`npm run qdrant:init`），幂等创建 collection + payload index，读 `QDRANT_*` 环境变量。应用启动时也会自检，缺 collection 则降级为"记忆仅落 MySQL"并 warn（不阻塞引擎启动）。

---

## 4. REST / WebSocket API 草案

### 4.1 M4.2.1 WebSocket

```
WS  /ws/engine?scene_id=<id>       订阅指定场景事件
事件类型同 bus：tick.start / tick.npc.updated / tick.end / error
```

**消息格式**：
```json
{ "type": "tick.npc.updated", "scene_id": 1, "tick": 5, "npc_id": 2, "meta": { ... } }
```

前端以此替代 3s 轮询；仍保留 `GET /api/engine/status` 作为 HTTP 降级通道。

### 4.2 M4.2.2 记忆

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/npc/:id/memories?limit=50&type=&since=` | 分页查 NPC 记忆 |
| POST | `/api/npc/:id/memories` | 人工追加记忆（body: `{content, type?, importance?}`） |
| DELETE | `/api/npc/:id/memories/:mid` | 删除单条 |

### 4.3 M4.2.3 反思

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/npc/:id/reflections?limit=20` | 查反思列表 |
| POST | `/api/engine/reflect` `{scene_id, npc_id?}` | 手动触发一次反思（不等 N tick） |

### 4.4 M4.2.4 事件

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/scene/:id/events?limit=50` | 查场景事件流 |
| POST | `/api/scene/:id/events` `{type, content, actor?, visible_npcs?, payload?}` | 注入事件 |
| DELETE | `/api/scene/:id/events/:eid` | 删除事件（调试用） |

---

## 5. 引擎内部变更

### 5.1 推理图扩展（graph/build.ts）

现有：`plan → speak → memory`  
M4.2 后：`[event-intake] → [memory-retrieve] → plan → speak → memory-store → [reflect?]`

- `event-intake`（M4.2.4）：`SELECT FROM scene_event WHERE scene_id=? AND created_at > NOW()-60s AND consumed_tick IS NULL LIMIT 10` → 注入到 plan 的 user 提示。
- `memory-retrieve`（M4.2.2）：基于 NPC 当前 `plan` 文本做 embedding → `qdrant.search(filter: npc_id=X, limit=MEMORY_TOP_K)` → 用返回的 point_id 批量 `SELECT content FROM npc_memory WHERE id IN (...)` → 合并 `recent_memories` 字段送入 plan/speak。**Qdrant 不可达时降级**：退回 MySQL 按 `importance DESC, created_at DESC` 取 top-K（无语义，保证功能可用），并记 warn。
- `memory-store`（M4.2.2）：每 tick 把 `latest_say + latest_action` 拼成观察条目 → 先 `INSERT npc_memory` 拿 `id` → 异步 `embedText` + `qdrant.upsert(id, vec, payload)`。嵌入失败留 `embed_status='failed'` 由 cron 重嵌。
- `reflect?`（M4.2.3）：`if tick % REFLECT_EVERY_N_TICK === 0`，启动 reflect 节点拉近 20 条记忆，LLM 生成 3 条 `{theme, content}` 写 `npc_reflection` + 更新 `simulation_meta.plan`。

**每节点仍 zod 严格校验**；重试降级策略不变。

### 5.2 调度器（scheduler.ts）

- M4.2.1：在 `bus.emitEvent` 之后对接 WebSocket 发送器
- M4.2.1：`persistNpcTick` 时从 `ai_call_log` 汇总本 tick 的 `cost_usd_total`（按 npc 累加）
- M4.2.0：`metaStr.byteLength > META_SOFT_BYTES` 时不仅 `console.warn`，还把 warn 添加到 scheduler.status() 里，并在 API 响应加 `X-Meta-Warn: 1` 头
- M4.2.0：tick 前检查 `ai_config.budget_tokens_per_tick`，若上一 tick 超支则本 tick `skipped` 并记事件

### 5.3 llmClient.ts（M4.2.1）

- 调用 LLM 时本地用 `tiktoken` 预估 prompt tokens；拿到响应后读 `json.usage.{prompt_tokens,completion_tokens}`（OpenAI 兼容字段），存进 `ai_call_log`
- 根据 `ai_config.provider + model` 查内建单价表，计算 `cost_usd = prompt_tokens * in + completion_tokens * out`
- 向量化新增封装 `memoryEmbed(npcConfig, text)` 带 30 天磁盘缓存（避免重复 embed 相同文本）；缓存 key 含 `embed_model`，换模型自动失效
- 新增 `backend/src/engine/memory/qdrantClient.ts` 统一封装 `upsert/search/delete`，内部带连接超时 2s + 1 次重试；失败抛 `QdrantUnavailableError` 让调用方走 MySQL 降级

---

## 6. 前端变更

| 组件 | 变更 | 子里程碑 |
|---|---|---|
| `NpcForm.vue` | `simulation_meta` textarea 之下加 **字节计与 64KB/256KB 警示条** | M4.2.0 |
| `Sandbox.vue` | ① 把 3s 轮询换成 WebSocket 订阅；② 右侧加 **tick 时间线浮窗**（最近 20 tick × NPC × status/duration/cost） | M4.2.1 |
| 新 `MemoryDrawer.vue` | 从 NPC 节点右键菜单打开，分页查看 / 追加 / 删除记忆 | M4.2.2 |
| 新 `ReflectionDrawer.vue` | 同上，分 theme 展示反思 | M4.2.3 |
| `Sandbox.vue` | 顶栏新增 **事件注入按钮组**（☁️ 下雨 / ⚡ 剧情 / 💬 自定义） | M4.2.4 |

---

## 7. 里程碑拆分

| 代码 | 任务 | 预估 | 关键交付物 |
|---|---|---|---|
| **M4.2.0 ✅** | `X-Meta-Warn` + meta 前端校验 + `budget_tokens_per_tick` | 0.5 天 | 已完成：`migrate-m42.ts` + scheduler `meta_warns`/budget skip + NpcForm 字节计 + Sandbox 徽章 + 4 条单测 |
| **M4.2.1** | WebSocket + tokens/cost 真实化 + tick 时间线浮窗 | 2.5 天 | `/ws/engine` + `ai_call_log` 扩列 + `tiktoken` 接入 + 浮窗 |
| **M4.2.2** | 记忆向量化 M-1→M-2 | 4 天 | `npc_memory` 表 + `event-intake`/`memory-retrieve`/`memory-store` 三节点 + Memory REST + Drawer UI |
| **M4.2.3** | 反思循环 | 2.5 天 | `npc_reflection` 表 + reflect 节点 + REST + Drawer UI |
| **M4.2.4** | 事件总线 | 3 天 | `scene_event` 表 + REST + event-intake 集成 + 前端事件注入面板 |
| **总计** | — | **12.5 天** | — |

**评审通过后，推荐分 3 个 PR 批次提交**：

- PR-1（M4.2.0 + M4.2.1）：基础设施与观测性；4 天
- PR-2（M4.2.2 + M4.2.3）：智能升级；7 天
- PR-3（M4.2.4）：世界升级；3 天

---

## 8. 测试矩阵

| 层 | 用例 | 子里程碑 | 测试形式 |
|---|---|---|---|
| 数据 | 迁移幂等；软硬阈值边界 | 全部 | 单测（vitest）+ 手测 |
| 引擎 | memory-retrieve：Qdrant 返回 top-K 且按相似度降序；mock qdrant client | M4.2.2 | 单测 |
| 引擎 | memory-store：INSERT + upsert 双写；upsert 失败时 `embed_status='failed'` | M4.2.2 | 单测 |
| 引擎 | Qdrant 不可达时自动降级为 MySQL importance 排序，引擎不崩 | M4.2.2 | 单测（停掉 mock 客户端） |
| 引擎 | reflect 节点按 `REFLECT_EVERY_N_TICK` 触发 | M4.2.3 | 单测（跑 10 tick 验证触发次数） |
| 引擎 | event-intake 去重（`consumed_tick`）与可见性过滤 | M4.2.4 | 单测 |
| API | memories/reflections/events CRUD 错误码 | M4.2.2~4 | supertest |
| WS | 订阅/断线重连/多客户端广播 | M4.2.1 | node `ws` 客户端单测 |
| 性能 | 1 场景 × 8 NPC × 10 tick × memory_top_k=5：延迟/成本基线 | M4.2.2 | 手动 smoke（扩展 `smoke:engine`） |
| 回归 | M4.1 `dry_run` 与真 LLM smoke 继续 PASS | 全部 | `npm run smoke:engine` |

---

## 9. 风险与降级

| 风险 | 影响 | 缓解 |
|---|---|---|
| embedding API 费用膨胀 | 每 tick 多 N 次 embed 调用 | `memoryEmbed` 加磁盘缓存；`importance < 3` 的记忆不 embed |
| Qdrant 节点宕机 / 网络抖动 | 记忆检索失败 → 引擎 tick 报错 | `qdrantClient` 2s 超时 + 1 次重试；失败走 MySQL importance 降级检索；运维添加健康检查与重启策略 |
| MySQL 与 Qdrant 数据不一致（孤儿 point / 缺失 point） | 检索返回脏数据或为空 | 后台 cron `embed_status=failed/pending` 重试；提供 `npm run qdrant:reindex` 全量重建脚本；以 MySQL 为准 |
| Qdrant 磁盘膨胀（记忆只增不减） | 存储成本 | `MEMORY_RETENTION_DAYS=30` 之外批量 `qdrant.delete + MySQL DELETE`，由定时任务执行 |
| 反思节点输出偏离 JSON schema | tick 标错误 | 复用 M4.1.b 的 zod + retry1 降级 |
| 事件被 LLM 忽略 | 世界感知形同虚设 | prompts 里事件段单独列出并明确"请在 plan 中回应本次事件"；观察小样本后调整 |
| WebSocket 连接风暴 | 后端压力 | 限每场景 8 个订阅；超出 HTTP 降级 |
| 预算限流误伤 | NPC 被静默 | `budget_tokens_per_tick` 默认 2000（远高于当前 ~800 实测）；触发时打 warn 而非直接 skip 首次 |

---

## 10. 验收清单（Definition of Done）

- [x] `npm run db:migrate-m42` 幂等通过；*(脚本已交付，幂等检查列存在性)*
- [ ] 现有 `npm run smoke:engine` 真 LLM PASS 不退步；*(M4.2.0 纯后端数据 + 前端 UI，不影响 smoke)*
- [x] **M4.2.0**：发超软阈值 meta 时响应头含 `X-Meta-Warn: 1`；前端 NpcForm 实时显示字节计；`budget_tokens_per_tick` 超支触发 `skipped`；
- [ ] **M4.2.1**：前端 Sandbox 关闭 3s 轮询，改 WebSocket；每 tick 看到真实 `cost_usd > 0`；时间线浮窗展示最近 20 tick；
- [ ] **M4.2.2**：`npm run qdrant:init` 幂等通过；跑 10 tick 后 `npc_memory` 至少 20 条且 Qdrant collection 点数一致；`latest_say` 开始引用历史细节；Drawer UI 能查/增/删；停掉 Qdrant 后引擎继续可跑（降级 warn 出现在 status）；
- [ ] **M4.2.3**：每 5 tick 出现一条反思，`npc_reflection.theme` 覆盖 goal/emotion/relation 至少各 1；
- [ ] **M4.2.4**：前端点"下雨"按钮注入事件，下一 tick NPC `latest_say` 体现感知；
- [ ] 所有新接口覆盖错误码单测，后端测试总数 ≥ 50 条全绿；
- [ ] 文档：本文件推 `1.0 关盘版`；README 文档索引补链；`requirements-character-scene.md` 同步。

---

## 11. 评审投票模板

### 11.1 首轮评审结论（v0.2 已锁定）

| 评审项 | 结论 | 来源 |
|---|---|---|
| §7 执行顺序 | 按当前方案 **0 → 1 → 2 → 3 → 4** | 业主 |
| §3.1 记忆存储 | **切换 Qdrant**，放弃 MySQL 客户端余弦 / pgvector 备选 | 业主 |
| §2.2 环境变量默认值 | 先使用当前默认 | 业主 |
| §7 PR 批次切分 | 保持 3 批（不再拆细） | 业主 |
| §1.2 非范围 | 复审确认并补 3 条（动作副作用 / NPC 间对话管线 / 场景时间系统） | 业主 |
| §6 新字段 / API / UI | 暂无补充 | 业主 |

### 11.2 后续评审投票表（如有二轮意见）

| 反馈人 | 顺序（0→1→2→3→4 / 其它） | PR 批次（3 / 更细 / 一把梭） | 数据模型（§3） | API（§4） | 其它意见 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

---

## 附：与选型报告 / M4.1 细设字段对齐

| 本文档 | 引用 |
|---|---|
| §1 范围 | `engine-selection.md` §6 里程碑衔接 |
| §3 数据层 | `engine-integration-m4.1.md` §4 字段规范 |
| §5.1 推理图扩展 | `engine-selection.md` §5 推荐方案（含 reflect/memory 占位） |
| §5.3 llmClient 扩展 | `engine-integration-m4.1.md` §7 LangGraph 图 |
| §10 DoD | `engine-integration-m4.1.md` §14 DoD 继承 |
