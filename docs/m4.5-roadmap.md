# M4.5 路线图（v1.0 整节点关盘）

| 文档版本 | 日期 | 说明 |
|---|---|---|
| 0.1 | 2026-04-23 | **M4.5 评审稿**：承接 M4.4 整节点关盘（[`m4.4-summary.md`](./m4.4-summary.md) §7 N-1~N-6 + M4.2/M4.3 历史延后项 X-1~X-7 / L-3 / L-5）共 16 条延后项；梳理议题池 + 4 套组合方案 + 9 条关键设计决策（Q1-Q9）。仅规划，不动代码。 |
| 0.2 | 2026-04-23 | **评审结论录入（方案甲全推荐通过 / 全 a）**：Q1=a（方案甲：U-A 动态目标 + U-B 记忆时间感 + U-N soft window，2.9 天）、Q2=a（REST + scene_event 双通道）、Q3=a（独立 `npc_goal` 表）、Q4=a（event > goal > schedule 硬优先级）、Q5=a（reflect prompt + `npc_memory.slot_hour` 冗余列双管齐下）、Q6=a（单文件 `migrate-m45.ts` 幂等）、Q7=a（前端最小改动，气泡 `🎯 目标` 回退）、Q8=a（后端 ≥220 / 前端 ≥20）、Q9=a（3 批次：M4.5.0 + M4.5.1.a + M4.5.1.b）。**进入 M4.5.0 开发窗口**。 |
| 0.3 | 2026-04-23 | **M4.5.0 已交付**（commit `50c888b`）：U-B 记忆时间感 + U-N 日程 soft window 一次合并提交。落地 `npc_memory.slot_hour` 列 + `idx_npc_memory_npc_slot` 复合索引；新增 `schedule/softWindow.ts` 纯函数 + `MEMORY_SLOT_HOUR_ENABLED` / `SCHEDULE_SOFT_WINDOW_MIN` / `SIM_CLOCK_MINUTE` 三开关；`reflect` 节点 system prompt 注入【当前时段】；`storeMemory` / 反哺 memory 链路透传 `slotHour`；migrate-m45.ts 分段幂等；backend 单测 212 → 219（+11 softwindow / +3 memory slot）全绿。**下一步进入 M4.5.1.a（U-A 数据层 + REST）**。 |
| 0.4 | 2026-04-24 | **M4.5.1.a / M4.5.1.b 已交付**（commit `f251cec` / `13a0cb1`）：`npc_goal` 表 + `engine/goal/{config,fetch,crud}.ts` 三件套 + `POST/PATCH/GET/DELETE /api/engine/goals` 四个 REST；`scheduler` 按 `GOAL_ENABLED` 预取活跃目标；新增 `engine/graph/planPath.ts` 纯函数实现 event > goal > schedule > idle 四路决策；`plan` prompt 新增【当前目标】分支（goal 命中时抑制【当前时段计划】避免 LLM 分心）；`SimulationMetaV1` 扩 `plan_path` + `active_goal`，经 WS `tick.npc.updated` 广播；前端 `extractBubbleText` 升四级回退（say > action > goal > schedule）。**backend 251 / frontend 23 全绿；端到端真机冒烟 11/11 PASS（goal 下发下一 tick 切 plan_path='goal'、完成后立即回落 schedule 且 active_goal=null）。** 中途顺带交付"前台配置允许 IP 访问"（commit `18b6bf2`）：`vite.config.ts` 统一 `server.host: true` + `/api` `/uploads` `/ws` 同源代理，前端三条链路走相对路径即可跨设备联调。 |
| 1.0 | 2026-04-24 | **整节点关盘**：3 批次（M4.5.0 / M4.5.1.a / M4.5.1.b）+ 1 支线（IP 访问）合计 5 个 commit 全绿；后端单测 205 → **251**（+46，超 Q8=a 基线 +31）；前端单测 18 → **23**（+5，超 Q8=a 基线 +3）；新表 1 / 扩列 1 / 新 env 5 / 新 REST 4 / 新 WS 字段 2 / 新前端组件 0（持续 Q7=a 最小改动）。产出 [`m4.5-summary.md`](./m4.5-summary.md) + [`m4.5-showcase.md`](./m4.5-showcase.md)，tag `v0.4.5-closed`。 |

关联：[`m4.4-roadmap.md`](./m4.4-roadmap.md) v1.0 · [`m4.4-summary.md`](./m4.4-summary.md) · [`m4.3-summary.md`](./m4.3-summary.md) · [`m4.2-summary.md`](./m4.2-summary.md)

---

## 0. 摘要（TL;DR）

M4.4 交付后，NPC 有**日程骨架 + 对话可续接**，但仍缺 3 类能力：

1. **动态主动性** —— 日程是"硬编码 24 小时"，没有**临时目标**/**剧情插入**/**玩家干预**的覆盖机制（N-2）；NPC 的记忆里也没有"时间感"，反思写的都是"发生了什么"而非"我在什么时段做了什么"（N-1）
2. **工程可维护性** —— `Sandbox.vue` 从 M4.2 开始挂账至今已**延后 3 个节点**，1800+ 行单文件（X-3）；P95 observability 自 M4.2 起未落地（X-1）；反思周期仍然是全局一刀切（X-5）
3. **架构跃迁窗口** —— LangGraph.js 连续 2 节点挂账（M4.4 Q5=a）；现有 `graph/build.ts` 手写编排能跑但扩展性受限，尤其 M4.5 引入动态目标后，plan 节点分支会从「event vs schedule」升级到「event vs goal vs schedule」三路（Q5=a 是否还能顶？）

本节点目标：**从 3 类能力里挑 2 条落地，保持 2~3 天预算**。

---

## 1. 候选议题池

### 1.1 P0 · 强烈建议必做（解 M4.4 直接延后）

| ID | 议题 | 来源 | 成本 | 关键改动 |
|---|---|---|---:|---|
| **U-A** | **动态目标系统**（goal 覆盖 schedule） | M4.4 N-2 | 1.5~2 天 | 新表 `npc_goal(id, npc_id, title, kind, priority, expires_at, status, payload)`；`scheduler.tick` 优先查 active goal，命中则 goal 驱动 plan，否则落回 schedule；新 REST `POST/PATCH/GET /api/engine/goals` |
| **U-B** | **记忆带时间感**（reflect/summary 注入 time slot） | M4.4 N-1 | 0.5~0.8 天 | `reflect` 节点 system prompt 加「当前时段：<hour>:00-<hour+1>:00 <activity>@<location>」；`npc_memory.slot_hour TINYINT`（冗余列便于 RAG 过滤） |

### 1.2 P1 · 重点候选（能力跃迁 / 工程债务）

| ID | 议题 | 来源 | 成本 | 收益 |
|---|---|---|---:|---|
| **U-C** | **Sandbox.vue 组件拆分** | M4.2 X-3（已延后 3 节点） | 1 天 | 1800 行 → `Sandbox / SandboxPhaser / SandboxBubble / SandboxTimeline / SandboxEvents / SandboxGoals（新）` 6 组件；纯重构 0 新功能 |
| **U-D** | **观测 P95 histogram** | M4.2 X-1（已延后 3 节点） | 1 天 | tick / LLM / reflect 三路 bucket 统计；新 REST `GET /api/engine/metrics/histogram?range=1h`；前端可选小挂件 |
| **U-E** | **反思周期 per-NPC 差异化** | M4.2 X-5 | 0.5 天 | `npc` 表加 `reflect_config JSON`（可覆盖全局 `REFLECT_EVERY_N_TICK`）；低活跃 NPC 拉长周期省 token |
| **U-F** | **LangGraph.js 迁移** | M4.4 Q5=a 延后 | 2~3 天 | 重构 `engine/graph/build.ts` 为 DAG；condition edges 天然表达「event→speak / goal→plan / schedule→plan」；**风险**：学习曲线 + 回归面大 |

### 1.3 P2 · 轻量优化（可拼凑）

| ID | 议题 | 来源 | 成本 | 备注 |
|---|---|---|---:|---|
| U-G | 事件衰减 / 过期清理 | M4.2 X-7 | 0.5 天 | cron 或 tick 级老化 `scene_event`，防表无限增长 |
| U-H | embed collection 维度一致性保护 | M4.2 X-3 | 0.3 天 | 启动时校验 env 与 DB dim，不一致 fail-fast |
| U-I | 前端 🧠 降级窗口可配置 | M4.2 X-4 | 0.2 天 | 5min 硬编码改 env |
| U-J | 事件抽屉「删除」入口 | M4.2 X-6 | 0.3 天 | 后端已有 API，补 UI + 二次确认 |
| U-K | `tick.npc.updated` meta 分级 | M4.2 X-2 | 0.5 天 | 当前仅 summary；加 verbose / silent |
| U-L | L-3 `conv_turn` null parent 统一兜底 | M4.3 L-3 | 0.2 天 | 仅修计算逻辑 |
| U-M | L-5 前端 WeakMap 持久 parent 索引 | M4.3 L-5 | 0.5 天 | ring 超限后仍能查 parent |
| U-N | 日程 soft window 15min 插值 | M4.4 N-4 | 0.5 天 | 整点切换不再突变 |
| U-O | `SCHEDULE_LLM_HINT=true` 并行通道 | M4.4 N-3 | 0.3 天 | 作 user 消息 hint 而非 system 硬注 |
| U-P | 气泡 tooltip 展示 priority / 下一时段 | M4.4 N-5 | 0.3 天 | 鼠标悬停看完整日程窗 |

### 1.4 P3 · 架构/重构窗口（风险较大）

| ID | 议题 | 成本 | 备注 |
|---|---|---:|---|
| U-Q | 对话 session 聚合表 | 1.5 天 | M4.3 一直延后；引入 `conversation(id, scene_id, root_event_id, last_turn_at, status)` 会话起止追踪 |
| U-R | 玩家插话 / @mention 路由 | 1 天 | M4.3 P5=a 延后项；`plan.say.target` 重启 |

---

## 2. 组合方案（4 套）

### 方案甲 · 主动性加深（推荐，2.5~3 天）

```
U-A（动态目标系统）→ U-B（记忆带时间感）→ U-N（日程 soft window）
```

- **逻辑**：承接 M4.4 的日程骨架，补齐"目标覆盖 + 记忆时间感 + 整点平滑"三件套，NPC 从"有日程但机械"升级到"会被剧情/玩家动态影响"
- **收益**：**演示效果再跃迁**——玩家可通过 REST 给 NPC 下临时目标（如"去找小美聊聊"），NPC 当下放弃日程响应；反思里也能体现"下午我在书房读书时小美来找过我"
- **风险**：U-A 新表 + 新接口 + plan 节点前置分支从 2 路变 3 路，回归面中等；U-B 改 reflect 可能影响现有 summary 质量，需分批 live smoke
- **预估**：1.8 + 0.6 + 0.5 = **2.9 天**（含 live smoke）

### 方案乙 · 工程债务清理（1.5~2 天）

```
U-C（Sandbox.vue 拆分）+ U-D（观测 P95）+ U-E（反思差异化）
```

- **逻辑**：集中清 M4.2 三项核心延后（X-3/X-1/X-5），**0 新功能、0 用户可见变化**
- **收益**：后续迭代速度显著提升；观测接入后可客观评估成本优化效果
- **风险**：纯工程不刺激，但 X-3 已延后 3 节点，再不做容易积成技术债翻车
- **预估**：1 + 0.8 + 0.4 = **2.2 天**

### 方案丙 · 架构跃迁（3 天，高风险）

```
U-F（LangGraph.js 迁移）+ U-L/U-O（轻量搭配）
```

- **逻辑**：趁 M4.5 没有巨大新功能压力，一次性把图引擎换掉，**为 M4.6+ 多 agent / 并行 / 分支留空间**
- **收益**：架构债务一次清掉；condition edges 让 plan 分支从命令式变声明式
- **风险**：LangGraph.js 学习曲线 + 全量回归（现有 152+ 单测要逐条对齐）；**若回归不完整，M4.6 会持续吃成本**
- **预估**：2.5 + 0.3 + 0.3 = **3.1 天**（**无 buffer**）

### 方案丁 · 主动性 + 工程混合（2.5 天）

```
U-A（动态目标）+ U-C（Sandbox 拆分）
```

- **逻辑**：主动性只做最核心的 U-A，工程只动最紧的 U-C
- **收益**：NPC 主动性 + 前端可维护性双线推进
- **风险**：U-A 的 reflect/memory 没升级，NPC 还是"对目标机械执行"；Sandbox 拆分后可能需要 0.3 天补 bug
- **预估**：1.8 + 1 = **2.8 天**

---

## 3. 推荐：方案甲（2.9 天）

**理由**：
1. **承前启后**：U-A 是 N-2 的直接兑付，承接 M4.4 日程骨架最自然；U-B 打通 N-1 让记忆首次带"时间维度"，给 M4.6+ 的长期 arc 打基础
2. **演示价值**：REST 下发临时目标能给 stakeholder 展示一个**交互式** NPC 行为，而非只是"自动生活"
3. **边际收益高**：U-N（soft window）是 0.5 天的小补丁，把 M4.4 N-4 一并清掉不划算也不浪费
4. **风险可控**：三个子项都是"增量 + 回退通道可设"，符合 M4.3 / M4.4 一脉相承的设计风格

**批次划分**：
- **M4.5.0**（U-B + U-N，0.8~1 天）：先做相对轻量的记忆时间感 + soft window，铺垫主动性
- **M4.5.1.a**（U-A 数据层 + REST，0.8 天）：`npc_goal` 建表 + seed + CRUD 接口
- **M4.5.1.b**（U-A plan 节点三路分支 + 前端气泡/列表，1~1.2 天）：`event > goal > schedule` 优先级；前端新增 `SandboxGoals` 小组件（可选）或列入 `Sandbox.vue` 内联展示

---

## 4. 关键设计决策（Q1~Q9）

| # | 问题 | a（推荐） | b | c |
|---|---|---|---|---|
| **Q1** | 范围选型 | **方案甲 · 主动性加深（U-A + U-B + U-N，2.9 天）** | 方案乙 · 工程债务清理 | 方案丙 · LangGraph 迁移 |
| **Q2** | U-A goal 触发源 | **REST 接口 + scene_event 复用（两者均可下发，REST 为主）** | 仅 REST（scene_event 不承担 goal 生成） | 让 LLM 在 reflect 节点自主生成（风险高） |
| **Q3** | U-A goal 数据模型 | **独立 `npc_goal` 表**（npc_id/kind/title/priority/expires_at/status/payload JSON） | 复用 `npc_schedule` 扩列（priority ≥ 8 视为 goal） | 复用 `scene_event.type='goal'` 加 payload |
| **Q4** | U-A plan 分支优先级 | **三路硬优先级：event（响应式） > goal（主动覆盖）> schedule（背景）** | 加权融合（goal 作为高优先级 hint 与 schedule 并注） | LLM 自主裁决（返回 path 由 meta 标记） |
| **Q5** | U-B 记忆时间感注入方式 | **双管齐下：reflect 节点 system prompt 加 slot + `npc_memory.slot_hour` 冗余列** | 仅 prompt 注入（不加列） | 仅加列（不改 prompt，只供后续 RAG 过滤） |
| **Q6** | 迁移组织 | **单文件 `migrate-m45.ts` 幂等**（续 M4.3/M4.4 风格） | 拆两文件（.0 + .1 分开） | 嵌到 knex migrations |
| **Q7** | 前端策略 | **最小改动**：`Sandbox.vue` 内联列出 active goals；气泡加 `🎯 目标: <title>` 回退（优先级 say > action > goal > schedule） | 新建 `SandboxGoals.vue` 独立组件（小抽屉） | 不动前端（纯后端能力） |
| **Q8** | 单测基线 | **后端 ≥220 / 前端 ≥20** | 后端 ≥215 / 前端 ≥19 | 后端 ≥225 / 前端 ≥21 |
| **Q9** | 子节点数 | **3 批次：M4.5.0（U-B+U-N）+ M4.5.1.a（U-A 数据层）+ M4.5.1.b（U-A plan+前端）** | 2 批次（把 .1.a 并到 .0） | 4 批次（更细粒度） |

---

## 5. U-A 数据模型草图（Q3=a 情景）

```sql
CREATE TABLE npc_goal (
  id          BIGINT PRIMARY KEY AUTO_INCREMENT,
  npc_id      BIGINT NOT NULL,
  title       VARCHAR(128) NOT NULL COMMENT '目标短文本，直接给 LLM',
  kind        ENUM('scene','player','npc','self') NOT NULL DEFAULT 'player'
              COMMENT '来源：场景事件触发 / 玩家下发 / 其他 NPC 驱动 / 自主产生',
  priority    TINYINT NOT NULL DEFAULT 8 COMMENT '1..10；≥ schedule.priority 即覆盖',
  status      ENUM('active','paused','done','dropped') NOT NULL DEFAULT 'active',
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  expires_at  DATETIME DEFAULT NULL COMMENT '过期自动切 done；NULL 表示手动关闭',
  payload     JSON DEFAULT NULL COMMENT '附加上下文：target_npc_id / target_location / 进度信息',
  INDEX idx_npc_goal_npc_status (npc_id, status),
  INDEX idx_npc_goal_expires (expires_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**Plan 分支伪代码**（Q4=a）：

```ts
// scheduler.tick() NPC loop
const activeGoal = await fetchActiveGoalForNpc(npc.id);  // ORDER BY priority DESC LIMIT 1
const hasEvents = (eventItems?.length ?? 0) > 0;

let planPath: 'event' | 'goal' | 'schedule' | 'idle';
if (hasEvents) {
  planPath = 'event';  // M4.3 行为，不注入 goal/schedule
} else if (activeGoal && activeGoal.priority >= (scheduledActivity?.priority ?? 0)) {
  planPath = 'goal';   // 注入 goal.title 到 system prompt
} else if (scheduledActivity) {
  planPath = 'schedule';  // M4.4 行为
} else {
  planPath = 'idle';
}
nextMeta.plan_path = planPath;  // 前端可视化用
```

---

## 6. REST 接口草图（Q2=a 情景）

```
POST   /api/engine/goals              # 下发目标
       body: { npc_id, title, kind?, priority?, expires_in_seconds?, payload? }
       resp: { id, ...goal }

PATCH  /api/engine/goals/:id          # 更新状态/优先级
       body: { status? | priority? | expires_at? }
       resp: { id, ...goal }

GET    /api/engine/goals?npc_id=X&status=active
       resp: { items: [...], total }

DELETE /api/engine/goals/:id          # 硬删（status=dropped 的变体）
```

**下发示例**：
```bash
curl -X POST http://localhost:3000/api/engine/goals \
  -H 'Content-Type: application/json' \
  -d '{"npc_id":1,"title":"去图书馆找小美聊一聊","priority":9,"expires_in_seconds":300}'
# → 下一 tick 小明的 plan prompt 将带「【当前目标】去图书馆找小美聊一聊（优先级 9）」
```

---

## 7. 回退通道（与 M4.3/M4.4 一致）

| 开关 | 默认 | 效果 |
|---|---|---|
| `GOAL_ENABLED` | true | false 时 scheduler 不查 `npc_goal`，plan 回 M4.4 行为 |
| `MEMORY_SLOT_HOUR_ENABLED` | true | false 时 reflect 不注入 slot，`npc_memory.slot_hour` 仍写但不用 |
| `SCHEDULE_SOFT_WINDOW_MIN` | 15 | 设为 0 关闭 soft window，整点硬切 |

---

## 8. 单测规划（Q8=a 情景）

| 子项 | 新增测试 | 测试点 |
|---|---:|---|
| U-B reflect slot 注入 | +3 | 有 schedule/无 schedule/slot_hour 冗余列写入 |
| U-N soft window | +4 | 整点前后 7.5 分钟过渡；window=0 关闭 |
| U-A goal CRUD | +5 | POST/PATCH/GET/expires_at 自动 done/priority 排序 |
| U-A plan 分支 | +3 | event > goal > schedule 优先级；priority=0 降级 |
| U-A scheduler 集成 | +2 | fetchActiveGoal 降级；GOAL_ENABLED=false 跳过 |
| 前端 goal 气泡 | +2 | say > action > goal > schedule 四级回退 |
| **合计** | **后端 +17 / 前端 +2** | 205 + 17 = **222 后端** / 18 + 2 = **20 前端** |

---

## 9. 验收清单（预写，M4.5 关盘时对账）

### 9.1 M4.5.0（U-B + U-N）
- [x] 迁移幂等 2 次
- [x] reflect prompt 含 `【当前时段】<hour>:00 <activity>@<location>`
- [x] `npc_memory.slot_hour` 与 tick hour 一致
- [x] soft window：整点前 7 分钟 plan prompt 已切到下一时段模板
- [x] 后端 ≥213（实 219）

### 9.2 M4.5.1.a（U-A 数据层 + REST）
- [x] `npc_goal` 建表 + 4 REST 跑通
- [x] `expires_at` 过期自动切 `status='done'`（list/get 入口懒切换，见 `goal/crud.expireLapsedGoals`）
- [x] 后端 ≥218（实 240）

### 9.3 M4.5.1.b（U-A plan + 前端）
- [x] event > goal > schedule 四路（含 idle）分支可复现（`engine/graph/planPath.ts` 纯函数 + 6 单测）
- [x] `nextMeta.plan_path` + `active_goal` 经 WS `tick.npc.updated` 广播；REST `/engine/ticks` 可回放
- [x] 气泡四级回退（say > action > goal > schedule）
- [x] 后端 ≥222 / 前端 ≥20（实 251 / 23）

### 9.4 整节点
- [x] 3 批次全绿（M4.5.0 `50c888b` / M4.5.1.a `f251cec` / M4.5.1.b `13a0cb1`）
- [x] live smoke：下发 goal → plan_path 切 `goal`，active_goal 三字段对齐 → PATCH done → plan_path 回落 `schedule` 且 active_goal=null（11/11 PASS）
- [x] `docs/m4.5-summary.md` + `docs/m4.5-showcase.md` 产出
- [x] `docs/m4.5-roadmap.md` 更新到 v1.0 关盘
- [ ] `v0.4.5-closed` tag（本次收尾 commit 提交后立即打）

---

## 10. 延后明确（M4.5 不做）

| # | 项 | 去处 |
|---|---|---|
| 1 | LangGraph.js 迁移（U-F） | M4.6+ 评审再议 |
| 2 | 对话 session 聚合表（U-Q） | M4.6 或 M5.x |
| 3 | 玩家插话 @mention（U-R） | M4.6 |
| 4 | 观测 P95 histogram（U-D） | M4.6 强制优先 |
| 5 | Sandbox.vue 拆分（U-C） | M4.6 强制优先（已延后 4 节点，再不做将强制约束新功能） |
| 6 | 反思差异化（U-E）、事件衰减（U-G）、embed 维度校验（U-H）、前端 🧠 窗口（U-I）、事件抽屉删除（U-J）、meta 分级（U-K）、L-3/L-5 兜底、LLM hint（U-O）、priority tooltip（U-P） | M4.6 机会窗口 |

---

**M4.5 v0.1 评审稿，请逐项投票 Q1~Q9。如全默认直接回「全 a」，将锁定为方案甲并进入 v0.2 定稿阶段。**

---

## 11. v0.2 定稿补充（全 a 锁定）

### 11.1 决策结论一览

| # | 问题 | 定稿 | 理由 / 落地锚点 |
|---|---|---|---|
| Q1 | 范围 | **a：方案甲 U-A + U-B + U-N（2.9 天）** | 承接 M4.4 日程骨架最自然；演示价值高 |
| Q2 | goal 触发源 | **a：REST 主 + scene_event 复用** | 保持双通道灵活性；未来 scene 事件可派生 goal |
| Q3 | goal 数据模型 | **a：独立 `npc_goal` 表** | 与 `npc_schedule` 职责正交；expires_at 机制独立 |
| Q4 | plan 三路优先级 | **a：event > goal > schedule 硬优先级** | 可解释、可单测；避免加权模型黑盒 |
| Q5 | 记忆时间感 | **a：reflect prompt + `npc_memory.slot_hour` 冗余列** | prompt 驱动效果 + 冗余列便于 RAG 过滤；二者成本叠加低 |
| Q6 | 迁移 | **a：单文件 `migrate-m45.ts` 幂等** | 续 M4.3/M4.4 风格 |
| Q7 | 前端 | **a：最小改动，气泡 `🎯 目标` 四级回退** | Sandbox.vue 拆分归入 M4.6 强制优先级 |
| Q8 | 单测基线 | **a：后端 ≥220 / 前端 ≥20** | +15 后端 / +2 前端，符合 P0 规模 |
| Q9 | 子节点 | **a：3 批次** | 数据层 / plan / 前端分批推进降低回归面 |

### 11.2 执行顺序

```
M4.5.0（U-B + U-N，~1 天）
   │
   ├─ .0.a reflect prompt slot + npc_memory.slot_hour 迁移 + 3 单测
   └─ .0.b schedule soft window 15min 插值 + 4 单测
                │
                ▼
M4.5.1.a（U-A 数据层 + REST，~0.8 天）
   │
   ├─ npc_goal 建表（migrate-m45.ts 续写）+ seed skip（不同 M4.4.1.a，无默认 goal）
   ├─ engine/goal/{config,fetch,crud}.ts 三件套
   ├─ POST/PATCH/GET/DELETE /api/engine/goals 4 个 REST
   └─ +5 单测
                │
                ▼
M4.5.1.b（U-A plan 三路 + 前端，~1.1 天）
   │
   ├─ scheduler.tick 加 fetchActiveGoalForNpc + 3 路分支
   ├─ plan prompt 新分支注入「【当前目标】<title>（优先级 <p>）」
   ├─ nextMeta.plan_path（event/goal/schedule/idle）+ WS 广播
   ├─ 前端 Sandbox.vue 气泡四级回退（say > action > goal > schedule）
   └─ +5 后端单测 / +2 前端单测
```

### 11.3 关键约束回顾（锁定后不再变）

1. **无新前端组件**（Q7=a）：`Sandbox.vue` 内联展示 goals；新 `SandboxGoals.vue` 放弃，等 M4.6 拆分时一起重排
2. **无 LangGraph 迁移**（U-F 延后）：三路分支用命令式 if-else 写在 scheduler + runGraph，暂不引入 DAG
3. **3 个回退开关全部实现**：`GOAL_ENABLED` / `MEMORY_SLOT_HOUR_ENABLED` / `SCHEDULE_SOFT_WINDOW_MIN`
4. **迁移幂等**：`migrate-m45.ts` 分 2 段（M4.5.0 段改 `npc_memory` 扩列；M4.5.1.a 段建 `npc_goal` 表）

---

## 12. M4.5.0 已交付实录（v0.3）

> commit [`50c888b`](../backend) 一次 feat 合并 U-B + U-N；后端单测 **212 → 219 全绿**；迁移幂等 2 次验证通过。

### 12.1 实际落地清单

| 子项 | 交付 | 文件 |
|---|---|---|
| U-B 迁移 | `npc_memory.slot_hour TINYINT NULL` + `idx_npc_memory_npc_slot (npc_id, slot_hour)` 复合索引 | `backend/src/db/migrate-m45.ts` |
| U-B store 写入 | `storeMemory.StoreInput` 新增 `slotHour`；INSERT 末位字段持久化；越界 / 开关关 → 写 NULL | `backend/src/engine/memory/store.ts` |
| U-B reflect prompt | `buildReflectionPrompt` 新增 `currentSlot`，注入【当前时段】`HH:00-HH+1:00 <activity> @ <location>` | `backend/src/engine/reflection/prompts.ts` |
| U-B reflect 反哺 | `reflectIfTriggered` 新增 `slotHour` + `scheduledActivity`；衍生 memory 继承 slot_hour | `backend/src/engine/reflection/reflect.ts` |
| U-N 纯函数 | `computeEffectiveHour` + `resolveEffectiveHourFromClock`（跨日绕环 / 非法值降级） | `backend/src/engine/schedule/softWindow.ts` |
| U-N 配置 | `SCHEDULE_SOFT_WINDOW_MIN` 默认 15；[0,30] 外降级为 0 | `backend/src/engine/schedule/config.ts` |
| U-N scheduler 集成 | `effectiveHour = resolveEffectiveHourFromClock(SIM/now, windowMin)` 统一驱动 `fetchScheduleForNpc` + `GraphInput.currentSlotHour` | `backend/src/engine/scheduler.ts` |
| Graph 透传 | `GraphInput.currentSlotHour` → `storeMemory(slotHour)` × 2 + `reflectIfTriggered(slotHour, scheduledActivity)` | `backend/src/engine/graph/build.ts` |
| env 开关 | `MEMORY_SLOT_HOUR_ENABLED=true` · `SCHEDULE_SOFT_WINDOW_MIN=15` · `SIM_CLOCK_MINUTE=` | `backend/.env` + `.env.example` |

### 12.2 验收项完成情况（对账 §9.1）

| 项 | 状态 | 证据 |
|---|---|---|
| 迁移幂等 2 次 | ✅ | 第二次执行打印 `⏭  npc_memory.slot_hour 已存在，跳过` |
| reflect prompt 含【当前时段】 | ✅ | smoke 脚本 `system.includes('【当前时段】') === true`；样例：`【当前时段】15:00-16:00 吃下午茶 @ 阳台` |
| `npc_memory.slot_hour` 与 tick hour 一致 | ✅ | smoke 三分支：开关 ON+slot=10→10 / ON+slot=24→null / OFF+slot=10→null |
| soft window 切换 | ✅ | 10:44→10 / 10:45→11 / 10:59→11 / 23:50→0（跨日绕环）|
| 后端 ≥213 | ✅ **超额**（219）| softwindow +11 / memory slot +3 |

### 12.3 新增环境变量

```env
SCHEDULE_SOFT_WINDOW_MIN=15   # 0 = 禁用；[0,30] 外降级为 0
MEMORY_SLOT_HOUR_ENABLED=true # false 时 slot_hour 列写 NULL + reflect 不注入时段
# SIM_CLOCK_MINUTE=           # 0..59 注入仿真分钟（用于整点边界单测/演示）
```

### 12.4 回退通道现状

| 开关 | 默认 | 已实现 | 回退效果 |
|---|---|---|---|
| `MEMORY_SLOT_HOUR_ENABLED` | true | ✅ | 关时 slot_hour 列写 NULL、reflect 不注入【当前时段】 |
| `SCHEDULE_SOFT_WINDOW_MIN` | 15 | ✅ | 设 0 回 M4.4.1.b 整点硬切 |
| `GOAL_ENABLED` | — | ⏳ M4.5.1.a 落地 | 关时 scheduler 不查 `npc_goal` |

### 12.5 已知遗留（交给 M4.5.1）

- 尚未有 `npc_goal` 表与 CRUD REST（U-A 数据层）— ✅ **M4.5.1.a 已兑付（§13）**
- `plan` 节点仍为「event / schedule」二路（缺 goal 第三路）— ✅ **M4.5.1.b 已兑付（§14）**
- 前端暂无 `🎯 目标` 气泡 — ✅ **M4.5.1.b 已兑付（§14.4）**

---

## 13. M4.5.1.a 已交付实录（v0.4）

> commit [`f251cec`](../backend) 一次 feat 落地 U-A 数据层 + REST；后端单测 **219 → 240（+21）**；迁移幂等 2 次验证通过；`expires_at` 懒切换 + 关盘 GOAL_ENABLED=false 回退全通过。

### 13.1 实际落地清单

| 子项 | 交付 | 文件 |
|---|---|---|
| 建表迁移 | `npc_goal(id, npc_id, title, kind, priority, status, created_at, expires_at, payload)` + `idx_npc_goal_npc_status` + `idx_npc_goal_expires`；与 §5 草图字段 100% 对齐 | `backend/src/db/migrate-m45.ts` |
| 配置模块 | `getGoalConfig()` 读 `GOAL_ENABLED` / `GOAL_DEFAULT_TTL_SEC`（默认 true / 1800s）；env 缓存 + `resetGoalConfig()` 供单测 | `backend/src/engine/goal/config.ts` |
| 读路径 | `fetchActiveGoalForNpc(npc_id)` — `SELECT ... WHERE status='active' AND (expires_at IS NULL OR expires_at > NOW()) ORDER BY priority DESC` LIMIT 1；异常降级 null 不阻 tick | `backend/src/engine/goal/fetch.ts` |
| CRUD | `createGoal` / `patchGoal` / `listGoals` / `getGoalById` / `deleteGoal`；list/get 入口调 `expireLapsedGoals` 懒切换过期行为 `status='done'`；`GOAL_ENABLED=false` 时写操作拒绝（503 `GOAL_DISABLED`） | `backend/src/engine/goal/crud.ts` |
| REST | `POST / PATCH / GET / GET /:id / DELETE /api/engine/goals[?/:id]`；controller 层校验 + 错误码标准化 | `backend/src/controllers/goals.ts` · `backend/src/routes/engine.ts` |
| env 开关 | `GOAL_ENABLED=true` · `GOAL_DEFAULT_TTL_SEC=1800` | `backend/.env` + `.env.example` |
| 单测 | `engine-goal.test.ts`（config 4 · fetch 3 · crud 10 · REST 4）；覆盖 expires_at 懒切换 / priority 排序 / GOAL_ENABLED=false 拒写 / payload round-trip | `backend/tests/engine-goal.test.ts` |

### 13.2 验收项完成情况（对账 §9.2）

| 项 | 状态 | 证据 |
|---|---|---|
| `npc_goal` 建表 + 4 REST 跑通 | ✅ | smoke 脚本 POST → goal.id 自增；GET list pagination；PATCH status 切换；DELETE 幂等 |
| `expires_at` 过期自动切 `status='done'` | ✅ | `expireLapsedGoals` 在 list/getById 入口触发，UPDATE WHERE `status='active' AND expires_at<=NOW()` |
| 后端 ≥218 | ✅ **超额**（240）| +21 单测 |

---

## 14. M4.5.1.b 已交付实录（v0.4）

> commit [`13a0cb1`](../backend) 一次 feat 完成 plan 三路分支 + 前端四级气泡；后端单测 **240 → 251（+11）**；前端单测 **18 → 23（+5）**；端到端真机冒烟 11/11 PASS。

### 14.1 实际落地清单

| 子项 | 交付 | 文件 |
|---|---|---|
| 决策纯函数 | `computePlanPath({ hasEvents, activeGoal, scheduledActivity })` 返回 `'event' \| 'goal' \| 'schedule' \| 'idle'`；goal 仅在 `activeGoal.priority >= scheduledActivity.priority` 时压过 schedule | `backend/src/engine/graph/planPath.ts` |
| scheduler 接入 | tick 顶部按 `GOAL_ENABLED` 预取 `activeGoal = fetchActiveGoalForNpc(npc.id)`；NPC loop 将 goal 注入 `GraphInput.activeGoal` | `backend/src/engine/scheduler.ts` |
| graph 分支 | `runGraph` plan 节点：`planScheduledActivity = planPath==='schedule' ? input.scheduledActivity : null`；`planActiveGoal = planPath==='goal' ? input.activeGoal : null`；两者互斥注入 `buildPlanPrompt` | `backend/src/engine/graph/build.ts` |
| prompt 互斥 | `buildPlanPrompt({ scheduledActivity, activeGoal })`：title 非空字符串时注入【当前目标】`<title>`（优先级不显式出现，交给 LLM 语义推理），同时**显式关闭**【当前时段计划】；title 为空 trim 后落回 schedule 行为 | `backend/src/engine/graph/prompts.ts` |
| meta 广播 | `SimulationMetaV1` 扩 `plan_path?: 'event'\|'goal'\|'schedule'\|'idle'` + `active_goal?: { id, title, priority } \| null`；dry_run / live 两路均按 `computePlanPath` 结果落 meta；WS `tick.npc.updated.meta` 同步携带 | `backend/src/engine/types.ts` · `backend/src/engine/graph/build.ts` |
| 前端气泡 | `extractBubbleText(meta, replyTo?, scheduledActivity?, activeGoal?)` 四级回退 say > action > **goal**（`🎯 目标: <title>`）> schedule；`Sandbox.vue::refreshBubbles` 从 `simulation_meta.active_goal` 取值透传 | `frontend/src/utils/sandbox.ts` · `frontend/src/components/Sandbox.vue` |
| 后端单测 | `engine-plan-goal.test.ts`：`computePlanPath` 6 分支 + `buildPlanPrompt` 3（goal 注入 / 空 title fallback / 互斥关闭 schedule）+ `runGraph` 2（dry_run meta.plan_path + active_goal 写入）= 11 测 | `backend/tests/engine-plan-goal.test.ts` |
| 前端单测 | `sandbox.test.ts` +5：say > action > goal > schedule 四级；空 title goal 落回 schedule；null goal + null schedule 降级空串 | `frontend/src/utils/sandbox.test.ts` |
| 端到端真机冒烟 | 脚本 `tmp-e2e-m451b.ts`（已清理）按 id DESC 直查 `npc_tick_log` 绕过 tick 历史叠加；流程：start(dry_run) → 基线 id 记录 → POST goal(priority=9) → 等到 plan_path='goal' + active_goal 三字段对齐 → PATCH done → 等到 plan_path='schedule' + active_goal=null → stop + DELETE；**11 个断言全 PASS** | 日志见本 session |

### 14.2 验收项完成情况（对账 §9.3）

| 项 | 状态 | 证据 |
|---|---|---|
| event > goal > schedule > idle 四路分支 | ✅ | `planPath.ts` + 单测 6；冒烟覆盖 goal ↔ schedule 活切 |
| `plan_path` + `active_goal` 广播到 WS / REST | ✅ | `simulation_meta` / `tick.npc.updated.meta` / `/engine/ticks` 三处同步；冒烟直接消费 DB JSON 列验证 |
| 前端气泡四级回退 | ✅ | `extractBubbleText` + Sandbox 透传 + 5 新单测 |
| 后端 ≥222 / 前端 ≥20 | ✅ **超额**（251 / 23）| 本 section §14.1 单测列已列 |

### 14.3 新增 / 扩展字段

| 位置 | 字段 | 类型 | 说明 |
|---|---|---|---|
| `SimulationMetaV1` | `plan_path` | `'event' \| 'goal' \| 'schedule' \| 'idle'` | 本 tick plan 节点决策路径；前端可用于时间线徽章 / 调试面板 |
| `SimulationMetaV1` | `active_goal` | `{ id, title, priority } \| null` | 仅 `plan_path==='goal'` 时非 null；气泡取 `title` 渲染"🎯 目标: …" |

### 14.4 回退通道现状

| 开关 | 默认 | 已实现 | 回退效果 |
|---|---|---|---|
| `GOAL_ENABLED` | true | ✅ | 关时 scheduler 不查 `npc_goal`；plan 回 M4.5.0 行为（纯 event/schedule/idle 三路）；REST 写操作 503 |
| `MEMORY_SLOT_HOUR_ENABLED` | true | ✅（M4.5.0） | — |
| `SCHEDULE_SOFT_WINDOW_MIN` | 15 | ✅（M4.5.0） | — |

---

## 15. 整节点关盘总结

- **3 批次 + 1 支线** 全绿：M4.5.0 / M4.5.1.a / M4.5.1.b（核心）+ frontend IP 访问（支线）
- **后端** 单测 205 → **251（+46）**；**前端** 单测 18 → **23（+5）**；均超 Q8=a 基线
- **新 REST 4**（全部在 `/api/engine/goals*`）；**新 WS 字段 2**（`plan_path` / `active_goal`，沿用 `tick.npc.updated` 不开新通道）；**新前端组件 0**（持续 Q7=a 最小改动）
- **所有 M4.5 延后 Q/N 全兑付**；M4.5 遗留交给 M4.6（见 §10）

**M4.5 节点正式关盘，进入 M4.6 规划窗口。**
