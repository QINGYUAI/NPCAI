# M4.6 路线图：Sandbox 重构 + goal 自主 + P95 观测（方案丁）

| 文档版本 | 日期 | 说明 |
|---|---|---|
| 0.1 | 2026-04-24 | **M4.6 评审稿**：承接 M4.5 整节点关盘（[`m4.5-summary.md`](./m4.5-summary.md) §7 N-1~N-6 + roadmap §10 U-C/U-D/U-F/U-Q/U-R 等）共 **25 条延后项**；梳理议题池 + 5 套组合方案 + 9 条关键设计决策（Q1-Q9）。仅规划，不动代码。 |
| 0.1.observe | 2026-04-25 | **锁定方案己（观察期）**：暂不启动 M4.6 主线；做 0.3 天极小回补（N-5 priority 边界单测 + C-new-3 live goal smoke 工具脚本）。观察期 live smoke PASS，证明 M4.5 三路 plan_path 切换真机可观测。见 §12。 |
| **0.2** | **2026-04-25** | **定稿：锁定方案丁（U-C 深度重构 + N-1 自主 goal_done + U-D P95 观测，3.4 天）**。Q1=丁 / Q5=c 深度 / Q9=2 批；Q2=a·Q3=b·Q4=c **作为 M4.7 U-R+U-Q 启动预设**。v0.1 §四 的"按推荐方案甲"旧落地计划已**作废**（见 §13 新落地计划）。风险补 4 条（WS 重连 / MENTION 预设副作用 / 回填孤儿 / 并发 intake，见 §8）。Sandbox.vue 实测 **1977 行**（非 1800），U-C 工作量从 1.3 天 → 1.4 天。 |
| **0.2.1** | **2026-04-25** | **零改动核查后修订（§13.1.3 / §13.1.4 / §13.3）**：项目无 pinia 依赖 → 用 composables 代替；`sandbox.ts` 已存在 → 保留不动，仅新增 `sandboxWsRoute.ts`；单测基线 ≥47 → **≥48**；总规模 ≥50 → **≥51**。总预算 3.4 天不变。 |
| **0.2.2** | **2026-04-25** | **第三轮零改动核查：取消 `sandboxWsRoute.ts`**（`engine.ts::openEngineWs` 已内建 `switch(msg.type)` 分发到 6 种 handler，再包一层纯属重复造轮子）。配套调整：U-C 拆分步骤 **6 步 → 5 步**（原步骤 1+2 合并为"composables + engine handlers 工厂"）；新增 utils **1 → 0**（全部落在 composables）；U-C 预算 **1.4 → 1.3 天**（合并节省 0.1 天 + 吸收 1977 行带来的 0.1 天增量）；**总预算 3.4 → 3.3 天**。单测目标不变（composables 测试含 handler 分发边界）。 |
| **0.2.3** | **2026-04-25** | **第四轮零改动核查：气泡合并入 SandboxMap.vue**（气泡是 `scene.add.text / scene.add.container` 产生的 **Phaser GameObjects**，不是 Vue DOM，无 template，不能独立成 `<SandboxBubble>` 组件）。配套调整：§13.1.2 组件拆分表 SandboxMap+SandboxBubble **合为 SandboxMap.vue**（~500 行，Phaser 地图 + 节点 + 气泡三位一体）；组件数 **6 → 5**；U-C 拆分步骤 **5 → 4**（原步骤 3 SandboxBubble 取消，原步骤 4 / 5 顺序各 -1）；前端单测目标含 **6 mount 快照**（SandboxMap / SandboxGoals 展开渲染 / SandboxInput / SandboxControls 各 1 + Map bubble 显隐 2）；U-C 预算保持 **1.3 天**（工作量没变，只是组件合并）；**批次 A 已完成（8d030a5），批次 B 改为步骤 2 SandboxMap，批次 C 改为步骤 3+4+5**。 |

关联文档：[`m4.5-roadmap.md`](./m4.5-roadmap.md) v1.0 · [`m4.5-summary.md`](./m4.5-summary.md) · [`m4.5-showcase.md`](./m4.5-showcase.md) · [`m4.4-roadmap.md`](./m4.4-roadmap.md) · [`m4.3-roadmap.md`](./m4.3-roadmap.md)

---

## 0. 背景与总体约束

### 0.1 M4.5 关盘后的坐标

- **M4.5.0 U-B/U-N** 把"记忆时间感"和"日程软窗口"补齐；**M4.5.1.a/b U-A** 把"动态目标覆盖"从零做到生产可用（REST + plan 三路 + 前端气泡）
- 现场演示链路：玩家通过 REST 下 goal → NPC 下一 tick `plan_path='goal'` + 【当前目标】prompt → goal 完成 / 过期后回落 schedule
- **短板**（M4.5 关盘 summary §7 提出 + 本轮 C 阶段实测揭示）：
  1. **玩家没有对话入口**：现在玩家下 goal 是"写数据库"，不是"说话"；NPC 之间会彼此 @对方的 dialogue event，但玩家说话没法触达特定 NPC（U-R 自 M4.3 延后至今 **3 节点**）
  2. **对话缺会话元数据**：turn-taking 靠 `parent_event_id` 单链 + `conv_turn` 编号，**没有"会话"这个一等概念**，前端无法按"一次聊天"为单位展示 / 统计 / 归档（U-Q 自 M4.3 延后 **3 节点**）
  3. **goal 的"event 抢占"没有运维视角**：本轮 C1 live 实测发现 dialogue event 连续生成时 goal(priority=9) 永远没机会命中；设计正确，但运维侧看不见"现在 event 队列压着什么"（**C-new-1** 本轮新增）
  4. **Sandbox.vue 1800 行单文件** 已延后 **4 节点**，再不拆会成为 M4.6 新功能的硬约束（U-C）

### 0.2 M4.6 的定位

- **主题**：**玩家交互深化** — 玩家第一次能直接"@NPC 说话"并看到"一次完整会话"
- **不做**：LangGraph.js 迁移（U-F 继续延，等 M4.7+）/ 多 agent 并行（远期）/ 玩家多人同屏（远期）
- **选做（看 Q1 定）**：U-C Sandbox 拆分（已延后过多）、U-D P95 histogram 小窗口、N-5 priority range 校验

---

## 1. 议题池（25 条）

分级标准：**P0** 强烈建议（本节点必做）/ **P1** 重点候选（主线可选其一）/ **P2** 轻量优化（可拼凑）/ **P3** 架构级（延后）

### 1.1 P0 · 强烈建议（本节点主线）

| ID | 议题 | 来源 | 成本 | 关键改动 |
|---|---|---|---:|---|
| **U-R** | **玩家 @mention 路由**（重启 `plan.say.target`） | M4.3 P5=a 延后 3 节点 | 1 天 | 玩家消息带 `target_npc_id` → 写入 `scene_event.visible_npcs` 精确定向；`plan prompt` 读 target，被 @的 NPC 优先回复；未被 @的 NPC 正常走 schedule/goal |
| **U-Q** | **对话 session 聚合表** | M4.3 一直延后 3 节点 | 1.5 天 | 新表 `conversation(id, scene_id, root_event_id, participants JSON, last_turn_at, turn_count, status)`；`scene_event.conversation_id` 外键；自动按 lookback 开/闭；前端抽屉按会话分组展示 |

### 1.2 P1 · 重点候选（组合方案备选主线）

| ID | 议题 | 来源 | 成本 | 收益 |
|---|---|---|---:|---|
| **U-C** | **Sandbox.vue 组件拆分**（6 组件） | M4.2 X-3（**已延后 4 节点**） | 1 天 | 为新功能（MentionPicker / ConversationDrawer / GoalPanel）腾出空间 |
| **U-D** | **观测 P95 histogram** | M4.2 X-1（已延后 3 节点） | 1 天 | tick / LLM / reflect 三路分桶 + `GET /api/engine/metrics/histogram?range=1h` |
| **N-1** | **scheduler 主动裁决 goal 完成**（LLM self-report "goal_done"） | M4.5 N-1 | 0.8 天 | plan 输出新字段 `goal_done: boolean`，scheduler 捕获后 UPDATE `npc_goal.status='done'`；NPC "说出"完成 |
| **N-2** | **goal 驱动 Phaser 自动移动**（payload.target_location） | M4.5 N-2 | 1 天 | plan prompt 读 `payload.target_location`；新 `plan.move.to` 字段；前端 Phaser NPC 平滑移到目标坐标 |
| **N-3** | **scene_event → goal 自动派生**（双通道兑现） | M4.5 N-3 | 0.8 天 | scene_event 新 type='goal_hint'；scheduler 批量派生 `kind='scene'` goal 入 `npc_goal` |
| **U-F** | **LangGraph.js 迁移** | M4.4 Q5=a 延后 | 2~3 天 | 高风险，需独立节点评审 |

### 1.3 P2 · 轻量优化（可并入主线的小菜）

| ID | 议题 | 成本 | 备注 |
|---|---|---:|---|
| **N-4** | `plan_path` 徽章前端可视化 | — | **✅ 本节点开工前 C2 已兑现**（SandboxTimeline）；调试抽屉可在 U-C 拆分时补 |
| **N-5** | `npc_goal.priority` 1..10 range 校验（controller 层） | 0.2 天 | 直接补 |
| **U-E** | 反思周期 per-NPC 差异化（`npc.reflect_config JSON`） | 0.5 天 | 省 token |
| **U-G** | 事件衰减 / 过期清理（cron 或 tick 级老化 scene_event） | 0.5 天 | |
| **U-H** | embed collection 维度一致性保护（启动 fail-fast） | 0.3 天 | |
| **U-I** | 前端 🧠 降级窗口可配置（5min 硬编码 → env） | 0.2 天 | |
| **U-J** | 事件抽屉「删除」UI 入口 | 0.3 天 | |
| **U-K** | `tick.npc.updated` meta 分级（summary / verbose / silent） | 0.5 天 | 与 X-2 合并 |
| **U-L** | L-3 `conv_turn` null parent 统一兜底 | 0.2 天 | |
| **U-M** | L-5 前端 WeakMap 持久 parent 索引（ring 超限后仍可查） | 0.5 天 | |
| **U-O** | `SCHEDULE_LLM_HINT=true` 并行通道（user 消息 hint） | 0.3 天 | |
| **U-P** | 气泡 tooltip 展示 priority / 下一时段 | 0.3 天 | |

### 1.4 P3 · 架构 / 重构窗口（本节点延后）

| ID | 议题 | 成本 | 备注 |
|---|---|---:|---|
| **C-new-1** | dialogue event 抢占可观测性（队列深度 / 压制其他路径次数的埋点） | 0.5 天 | 本轮 C1 实测揭示；可附 U-D 一起做 |
| **C-new-2** | Sandbox 调试抽屉补 `plan_path` 徽章（Timeline 已有） | 0.2 天 | 与 U-C 合并 |
| **C-new-3** | live goal 真机 smoke 工具（暂停 dialogue + 下 goal + 验证 prompt） | 0.3 天 | ops 工具，可放本节点附录 |
| **X-8** | tick 粒度 trace id 贯穿（ai_call_log ↔ npc_tick_log ↔ memory ↔ event） | 1 天 | M4.3 部分落，M4.6 可进一步贯通 |

---

## 2. 组合方案（5 套）

### 方案甲 · 玩家交互深化（推荐，2.3~2.8 天）

```
U-R（@mention 路由）→ U-Q（conversation 聚合表）→ N-5（priority range 校验，收尾小项）
```

- **逻辑**：一次性把"玩家说话"（对话入口）和"会话"（对话元数据）两个长期欠账补齐；NPC 之间已有的 dialogue event 链天然兼容新表（只加 `conversation_id` 列），零中断
- **收益**：
  - 玩家第一次可以**直接跟 NPC 对话**（不再仅能写 goal）
  - 前端抽屉按"会话"分组展示，UX 对标成熟产品（ChatGPT 会话列表）
  - 为 M5 多人玩家 / UGC 剧情编辑器铺直接路径
- **风险**：
  - U-Q 新表 + 历史 scene_event 回填 `conversation_id`（迁移脚本需写幂等/分批，避免锁表）
  - U-R 改 `plan prompt` 分支多（被 @ / 未被 @ / 多人同聊）
- **预估**：1 + 1.5 + 0.2 = **2.7 天**（含 live smoke，不含 buffer）

### 方案乙 · goal 系统全功能（2.5~3.2 天）

```
N-1（LLM self-report goal_done）→ N-2（goal→Phaser 移动）→ N-3（scene_event→goal 派生）
```

- **逻辑**：把 M4.5 的 goal 从"REST 数据层能用"跃迁到"NPC 能自主推进 + 能驱动画面 + 能被场景自动触发"三件套
- **收益**：演示效果最强，goal 系统从"写 DB 能看 prompt"变成"真实能看见 NPC 移动去完成"
- **风险**：N-2 依赖 Phaser 路径规划（当前仅坐标跳转）；N-1 要让 LLM 输出稳定 `goal_done` 字段（prompt 约束 + 解析容错）
- **预估**：0.8 + 1 + 0.8 + 0.3 buffer = **2.9 天**

### 方案丙 · 工程债务清理（2.3~2.8 天）

```
U-C（Sandbox.vue 拆分）+ U-D（观测 P95）+ N-5（priority 校验） + C-new-1（event 抢占埋点）
```

- **逻辑**：集中清延后 4 节点的 U-C 和延后 3 节点的 U-D；**0 新功能、0 用户可见变化**
- **收益**：为 M4.7/M5 拆分空间 + 观测抓手；U-D 还能回答"本节点交付实际 P95 是多少"
- **风险**：纯工程不出戏；U-C 拆分涉及 1800 行，回归面大
- **预估**：1 + 1 + 0.2 + 0.5 = **2.7 天**

### 方案丁 · 混合（一个强制优先 + 一个演示点 + 一个观察点，2.5~3 天）

```
U-C（Sandbox 拆分）+ N-1（goal 自主裁决）+ U-D（P95 小窗口）
```

- **逻辑**：一次吃掉一个强制优先 + 一个用户可见的演示点 + 一个观测抓手
- **收益**：均衡打法；Sandbox 拆分不再延后，N-1 提升 goal 演示闭环，P95 补齐 M4.2 老账
- **风险**：三件事独立，buffer 吃紧；U-C 拆分 + N-1 prompt 改动并行有小回归风险
- **预估**：1 + 0.8 + 1 + 0.2 buffer = **3.0 天**

### 方案戊 · 架构跃迁（3~3.5 天，高风险）

```
U-F（LangGraph.js 迁移）+ U-L（conv_turn 兜底）+ U-O（LLM hint 通道）
```

- **逻辑**：把 plan/goal/schedule 三路 DAG 化，为 M5 多 agent 并行 / 分支留空间
- **风险**：**无 buffer**；251 + 36 单测全量回归；LangGraph.js 学习曲线
- **预估**：2.5 + 0.3 + 0.3 = **3.1 天**

---

## 3. 关键设计决策（Q1~Q9）

> 评审阶段请对每条给出 a/b/c 选项；v0.2 定稿后锁定。

### Q1 · 整体范围（主线）

- **a**：方案甲 U-R + U-Q + N-5（玩家交互深化，2.7 天，推荐）
- **b**：方案乙 N-1 + N-2 + N-3（goal 系统全功能，2.9 天，演示效果最强）
- **c**：方案丙 U-C + U-D + N-5 + C-new-1（工程债务清理，2.7 天）
- **d**：方案丁 U-C + N-1 + U-D（混合，3.0 天）
- **e**：方案戊 U-F + U-L + U-O（LangGraph 迁移，3.1 天，高风险）

### Q2 · U-R @mention 路由形态（若 Q1=a/d）

- **a**：**前端显式字段** — 玩家发消息时必须选目标 NPC；`scene_event.payload.target_npc_id=X`；plan prompt 读 target（简单可靠）
- **b**：**LLM 自动识别** — 玩家自由文本里出现 "@NPC_NAME" → 后端正则解析 → target_npc_id（UX 好，但解析歧义）
- **c**：**双通道** — UI 选 + 文本 @ 都支持，UI 选优先

### Q3 · U-Q `conversation` 数据模型（若 Q1=a）

- **a**：**最小**：`(id, scene_id, root_event_id, last_turn_at, status ENUM('active','ended'))`，participants 靠 JOIN event 计算
- **b**：**标准**：在 a 基础上加 `participants JSON`、`turn_count INT`、`last_content VARCHAR(128)`（列表渲染时省 JOIN）
- **c**：**丰富**：在 b 基础上再加 `topic VARCHAR(64) NULL`（LLM 摘要）、`avg_interval_ms INT`（分析用）

### Q4 · U-Q 会话生命周期

- **a**：**自动**：基于 `EVENT_LOOKBACK_SECONDS` 窗口；lookback 内有新 event 则 `status=active`，超时自动 `ended`（简单）
- **b**：**LLM 识别**：prompt 输出 "conversation_done" 信号触发 `ended`
- **c**：**自动 + 玩家手动**：玩家可点"结束会话"强制归档（最接近产品直觉）

### Q5 · U-C Sandbox.vue 拆分（若 Q1 包含 U-C）

- **a**：**推迟** — Q1 非方案丙/丁时，U-C 继续延（第 5 次）
- **b**：**局部**：仅拆 conversation / @mention 相关代码为 `SandboxConversation.vue`，其他 5 组件留 M4.7
- **c**：**完整**：一次拆 6 组件（成本 1 天，风险中等）

### Q6 · 前端改动尺度

- **a**：**最小** — 复用现有组件，仅在输入区加 NPC 下拉 + 会话按会话在抽屉里分组（无新组件）
- **b**：**中等** — 新 `MentionPicker.vue` + `ConversationDrawer.vue` 两个小组件
- **c**：**较大** — 新 MentionPicker / ConversationDrawer / SandboxGoals 三组件（含 N-4 调试抽屉徽章）

### Q7 · 迁移组织

- **a**：单文件 `migrate-m46.ts`（历史惯例，M4.2~M4.5 均如此）
- **b**：分节 `migrate-m46-{0|1}.ts`（若子节点多且改表冲突）

### Q8 · 测试基线

- **a**：后端 ≥266（+15）/ 前端 ≥44（+8）
- **b**：后端 ≥275（+24）/ 前端 ≥50（+14）

### Q9 · 子节点数

- **a**：**2 批次** — M4.6.0 = U-Q（表 + 历史回填 + 前端列表），M4.6.1 = U-R（@mention + plan 分支 + N-5）
- **b**：**3 批次** — M4.6.0 = U-Q 数据层，M4.6.1 = U-Q 前端，M4.6.2 = U-R
- **c**：**1 批次** — 不分，整体推进

---

## 4. 落地计划（按推荐方案甲，**v0.2 已作废，见 §13**）

> ⚠️ 以下 4.1/4.2/4.3 为 v0.1 按"方案甲 U-R+U-Q"评审时的草案；v0.2 已改选方案丁，**本节整体不再生效**，仅保留供历史对照。**实际落地以 §13 为准**。

### 4.1 M4.6.0 · U-Q 对话 session 聚合（1.5 天）

| 项 | 内容 |
|---|---|
| 数据层 | 新表 `conversation(id, scene_id, root_event_id, participants JSON, last_turn_at, turn_count, status)`；`scene_event` 新列 `conversation_id BIGINT NULL`（外键不加，便于冷启动，同 `npc_goal` 惯例） |
| 迁移 | `migrate-m46.ts` 段 1：建表 + 加列 + 索引（`idx_conversation_scene_last_turn`、`idx_scene_event_conversation`） + **历史 scene_event 回填 conversation_id**（按 parent_event_id 追溯到 root，分批 1000 行避免锁表） |
| 后端 | `engine/conversation/crud.ts`（create / endByTimeout / listByScene）；`scheduler.intake` 在读取 dialogue event 时维护 last_turn_at 和 turn_count；基于 `EVENT_LOOKBACK_SECONDS` 超时自动 `ended` |
| REST | `GET /api/scene/:id/conversations?status=active&page=1&page_size=50`（返回 conversation + 最后 3 条 event 预览） |
| 前端 | `SandboxEvents.vue` 抽屉按会话分组展示（会话头部显示 participants + last_turn_at + turn_count） |
| 单测 | +8（crud 4 / REST 2 / 生命周期 2） |
| 验收 | 回填后所有现有对话链都有 conversation_id；lookback 超时自动 ended；前端抽屉按会话分组 |

### 4.2 M4.6.1 · U-R @mention 路由 + N-5 收尾（1 天）

| 项 | 内容 |
|---|---|
| 前端 | 输入区加 NPC 多选（复用现有 NPC 头像列表）；发送时 `scene_event.visible_npcs = [selected]` + `payload.target_npc_id = X`（单选优先） |
| 后端 | `engine/event/fetchRecentEvents.ts` 已支持 visible_npcs 过滤；`buildPlanPrompt` 扩参 `mentionedBy`：被 @ 的 NPC prompt 插入「【玩家直接@你】`<content>`（请优先回应）」 |
| plan 三路合流 | `computePlanPath` 扩一路 `mentioned`（优先级高于 event/goal/schedule）；顺序：`mentioned > event > goal > schedule > idle` |
| N-5 | goal controller 加 priority 1..10 range 校验（< 0.3 天，不单独子节点） |
| 单测 | +7（@mention plan prompt 3 / mentioned path 3 / N-5 range 1） |
| 验收 | 玩家 @小明 说话 → 小明下一 tick `plan_path='mentioned'` + prompt 含【玩家直接@你】；未被 @的 NPC 正常走 schedule/goal |

### 4.3 总规模预估

- 后端单测 251 → **≥266**（+15）
- 前端单测 36 → **≥44**（+8，含 C2 加的 13 条）
- 新表 **+1**（`conversation`）
- 扩列 **+1**（`scene_event.conversation_id`）
- 新索引 **+2**
- 新 REST **+1**（`GET /api/scene/:id/conversations`）
- 新 WS 字段 **+1**（`plan_path='mentioned'` 枚举；`meta.mentioned_by?: number`）
- 新前端组件 **0**（Q6=a 最小改动）
- 总耗时 **2.7 天**（含 live smoke，不含 buffer）

---

## 5. 数据模型草图（方案甲，**v0.2 作废** — 保留为 M4.7 U-Q 启动时的实现参考，见 §14）

```sql
-- [M4.6.0] 对话会话聚合
CREATE TABLE conversation (
  id               BIGINT PRIMARY KEY AUTO_INCREMENT,
  scene_id         BIGINT NOT NULL,
  root_event_id    BIGINT NOT NULL COMMENT '会话第一条 scene_event.id',
  participants     JSON   NOT NULL COMMENT '[npc_id, ...]；玩家按需用 0 或 -1 占位',
  last_turn_at     DATETIME NOT NULL,
  turn_count       INT NOT NULL DEFAULT 1,
  status           ENUM('active','ended') NOT NULL DEFAULT 'active',
  ended_at         DATETIME DEFAULT NULL,
  created_at       DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_conversation_scene_last (scene_id, last_turn_at DESC),
  INDEX idx_conversation_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
  COMMENT='[M4.6.0] 对话会话聚合表（由 scheduler 自动开闭）';

-- [M4.6.0] scene_event 归属会话
ALTER TABLE scene_event
  ADD COLUMN conversation_id BIGINT DEFAULT NULL
    COMMENT '所属会话 id（可 NULL：手插系统事件、历史数据未回填）',
  ADD INDEX idx_scene_event_conversation (conversation_id);

-- 历史回填（分批 1000 行）：按 parent_event_id 追溯根节点 → 同根节点归一个 conversation
-- 无 parent 的事件各自开新会话
```

---

## 6. 环境变量规划（方案甲，**v0.2 作废** — 保留为 M4.7 U-R+U-Q 启动参考，见 §14）

```env
# [M4.7 U-Q] 对话会话聚合（预设，v0.2 本节点不落地）
CONVERSATION_ENABLED=true                  # false 时 scheduler 不维护 conversation；REST 返空列表
CONVERSATION_IDLE_TIMEOUT_SEC=180          # 超过此阈值未新增 event 则 conversation.status='ended'（Q4=c 时：玩家可按钮强制 ended，覆盖此值）

# [M4.7 U-R] @mention 路由（预设）
MENTION_ENABLED=true                       # false 时忽略 target_npc_id，走原有 visible_npcs 过滤
MENTION_PROMPT_STRENGTH=strong             # strong / normal / off（控制 plan prompt 强度；⚠️ 风险见 §8 R-2）
```

**回退通道**：

| 开关 | 回退行为 |
|---|---|
| `CONVERSATION_ENABLED=false` | scheduler 不维护会话；`conversation_id` 恒为 NULL；REST 返空列表；前端回退 M4.5 平铺列表展示 |
| `MENTION_ENABLED=false` | plan 仍走 event > goal > schedule > idle 四路；不触发 `mentioned` 路径 |

---

## 7. 观测埋点（方案甲，**v0.2 作废** — 保留为 M4.7 启动参考）

| 类别 | 埋点 | 备注 |
|---|---|---|
| 日志 | `[conversation.crud] endByTimeout N 行` | 超时批量归档 |
| 日志 | `[conversation.intake] root=X 命中 last_turn_at` | 新 event 追加到已有会话 |
| 指标 | `conversation_active_gauge` | 仍活跃的 conversation 数（可 Prometheus 抓取） |
| 日志 | `[mention] target=X ticked on tick=T` | 被 @ NPC 的响应命中 |
| WS | `tick.npc.updated.meta.plan_path='mentioned'` | 前端徽章多一路 |

**方案丁实际观测埋点见 §13.3**。

---

## 8. 风险 & 缓解（方案丁视角，v0.2 已重写）

### 8.1 方案丁本节点风险

| # | 风险 | 缓解 |
|---|---|---|
| D-1 | **Sandbox.vue 拆分回归面大**（当前 1800 行，深拆 6 组件 + utils + 2 store） | 每拆一个组件跑完整 `vue-tsc` + `vitest`；先建立"拆分前 baseline 快照"（git stash 列差异），小步 commit（每个子组件独立 commit） |
| D-2 | **pinia store 拆 2 个后响应式触发链变化** | 新 store 构造函数写单测验证 getter 计算性；前端 `vue-tsc` 全绿 + 手动 2D 沙盒跑 live smoke 验证徽章/气泡/timeline 三个视图都刷新 |
| D-3 | **N-1 LLM goal_done 字段解析不稳**（prompt 有时不输出 / 输出错拼写） | 容错：`goal_done` 不存在或非 boolean 一律按 `false` 处理；改动 prompt 时先 dry-run 3 个 case 验证；保留 REST PATCH done 的外部关闭通道作为降级 |
| D-4 | **U-D P95 histogram 计算成本** | 使用 `mysql` 窗口函数（`PERCENT_RANK`）或本地 in-memory 滑动窗口；查询限 100 条窗口，避免全表扫 |
| D-5 | **U-D 前端观测条抢占 Sandbox 空间** | Q6=a 最小改动：复用现有 header，加一行 `ObserveBar.vue`（高 24px），可折叠 |

### 8.2 M4.7 预留风险（来自 v0.1 自审；方案甲启动时需消化）

| # | 风险 | 缓解 |
|---|---|---|
| R-1 | **历史 scene_event 回填锁表** | 分批 1000 行 + `SKIP LOCKED`；维护窗口执行 |
| R-2 | **`MENTION_PROMPT_STRENGTH=strong` 预设副作用**：LLM 被强制回应玩家，可能破坏 NPC 本身的 role / goal 一致性 | 默认值改回 `normal`；`strong` 仅作为 A/B 实验入口；提供 prompt 回归单测集 |
| R-3 | **历史 conversation 回填孤儿**：parent_event_id 链断裂的 event 会无 conversation_id | 回填脚本跑完后巡检：`SELECT COUNT(*) FROM scene_event WHERE conversation_id IS NULL AND type='dialogue' AND id > <baseline>`，人工看孤儿比例；大量孤儿则放宽规则（按场景 + 5min 窗口合并） |
| R-4 | **并发 intake 双写同一 conversation**：两个 NPC 同 tick 同时追加 event 到同一会话 | `INSERT ... ON DUPLICATE KEY UPDATE` 原子化 last_turn_at；或前置 SELECT FOR UPDATE |
| R-5 | **前端 WS 重连导致对话态错乱**：断连期间错过 N 条 event，重连后 conversation.turn_count 前端缓存过期 | WS `reconnect` 后主动 `GET /api/scene/:id/conversations?status=active` 重拉；前端缓存版本号匹配 |
| R-6 | **`plan_path='mentioned'` 与 event 冲突** | 顺序固定 `mentioned > event`；被 @ NPC 即使有其他 event 也优先回应玩家 |
| R-7 | **`conversation_id` 外键未加 → 数据不一致** | 巡检 SQL：`SELECT COUNT(*) FROM scene_event WHERE conversation_id NOT IN (SELECT id FROM conversation)` 应恒为 0 |

---

## 9. 验收标准（方案甲，**v0.2 作废** — 方案丁验收见 §13.4）

---

## 10. 延后明确（方案丁口径，v0.2 重写）

> 方案丁本节点做：U-C（Sandbox 深度重构）+ N-1（LLM 自主 goal_done）+ U-D（P95 观测）
> 方案丁本节点**不做**：U-R / U-Q / N-2 / N-3 / U-F 等

| # | 议题 | 延后到 | 备注 |
|---|---|---|---|
| 1 | **U-R @mention 路由 + U-Q 对话聚合** | M4.7 主线 | 已在 §14 写入预设（Q2=a / Q3=b / Q4=c），M4.7 启动即可按预设落地 |
| 2 | N-2 goal 驱动 Phaser 自动移动 | M4.7+ | 依赖 Phaser 路径规划；v0.1 自审认为成本被低估，需独立评审 |
| 3 | N-3 scene_event → goal 自动派生 | M4.7+ | 与 U-R 触发机制有冲突面，建议 U-R 落地后再做 |
| 4 | U-F LangGraph.js 迁移 | M5.x 独立节点 | 风险/收益评估仍不明，至少等 2 个节点再启 |
| 5 | N-5 goal priority 校验 | **已完成**（M4.5.observe commit `96f850d`） | — |
| 6 | C-new-3 live goal smoke 工具 | **已完成**（M4.5.observe commit `59ff5e8`） | — |
| 7 | C-new-1 event 抢占观察窗口 | M4.7 机会窗口 | 观察期已验证"event 消费完即让位 goal"，优先级从 P3 降至 P3-low |
| 8 | U-E/U-G/U-H/U-I/U-J/U-K/U-L/U-M/U-O/U-P/C-new-2/X-8 | M4.7+ 机会窗口 | 保留议题池 |

---

## 11. 评审请关注（v0.1 → v0.2 转正前，**v0.2 已完成**）

v0.2 定稿结果：Q1=丁 / Q5=c / Q9=2；Q2=a·Q3=b·Q4=c 作为 M4.7 预设；Q6/Q7/Q8 不适用（方案丁不新增前端大组件 / 不起新表 / 不涉单测阈值跃迁）。详见 §12。

---

## 12. 决策锁定（v0.2 定稿）

**选择**：**方案丁 — U-C 深度重构 + N-1 自主 goal_done + U-D P95 观测（3.3 天）**

### 12.1 决策摘要

| 决策点 | 选择 | 备注 |
|---|---|---|
| **Q1 主线** | **方案丁** | 观察期证明 M4.5 稳定后，用户综合考量后定向"先拆工程债 + 补 goal 自主闭环 + 补观测"；U-R/U-Q 留 M4.7 |
| **Q5 U-C 拆分范围** | **c 深度重构**（拆 6 组件 + 提取 utils + pinia 拆 2 store） | 比方案丁原 U-C 多 0.3 天；Sandbox.vue 从 1800 行降至**每组件 <400 行**，为 M4.7 U-R+U-Q 留空间 |
| **Q9 子节点数** | **2 批** | M4.6.0 = U-C / M4.6.1 = N-1 + U-D |
| ~~Q2 @mention 形态~~ | **a UI 显式**（**M4.7 预设**） | 本节点不落地；见 §14 |
| ~~Q3 conversation 模型~~ | **b 标准**（**M4.7 预设**） | 本节点不落地；见 §14 |
| ~~Q4 会话生命周期~~ | **c 自动 + 玩家手动**（**M4.7 预设**） | 本节点不落地；见 §14 |
| Q6 前端改动尺度 | 不适用 | 方案丁纯内部重构 + 新增 ObserveBar 微组件，不涉及 6 组件 vs 3 组件选择 |
| Q7 迁移组织 | 不适用 | 方案丁不新增表 / 不扩列 |
| Q8 测试基线 | 固定 | 后端 261 → ≥275（+14）/ 前端 36 → ≥50（+14，U-C 拆分后每组件最少 1 条） |

### 12.2 作废的 v0.1 章节

以下小节基于 v0.1 方案甲撰写，**v0.2 整体作废**，仅保留供 M4.7 U-R+U-Q 启动时参考：
- §4 落地计划（旧 M4.6.0 U-Q / M4.6.1 U-R+N-5）
- §5 数据模型草图（conversation 建表 / scene_event.conversation_id）
- §6 环境变量规划（CONVERSATION_* / MENTION_*）
- §7 观测埋点（方案甲口径）
- §9 验收标准（方案甲口径）

v0.2 以 §13 落地计划 + §14 M4.7 预留为准。

### 12.3 观察期回顾（M4.5.observe）

- ✅ N-5 priority 边界单测加固（11 条）
- ✅ C-new-3 live-goal-smoke 工具脚本 + `npm run smoke:live-goal`
- ✅ 真机 live smoke PASS：`tick#257(event) → tick#261(goal) → PATCH done → tick#266(event)`
- ✅ 证明三路 plan_path 切换真机可观测；event 抢占是**瞬时**的（消费完让位），不是永久压制

---

## 13. 落地计划（方案丁·v0.2 正式计划）

### 13.1 M4.6.0 · U-C Sandbox 深度重构（1.3 天，v0.2.2 修订）

#### 13.1.1 拆分前基线（v0.2.2 修订：1977 行 / 合并步骤后回到 1.3 天）

- `frontend/src/components/Sandbox.vue`：**1977 行**（`wc -l` 2026-04-25 实测；v0.1 评估时错记为 1800）
- 单文件承载：Phaser 初始化 / WS 连接 / NPC 状态管理 / 对话气泡 / 输入表单 / 目标面板 / 时间线入口 / 事件抽屉
- SandboxTimeline.vue 已在 M4.5.1.c 独立（不纳入本次拆分）
- **工作量修正链**：
  - v0.1：按 1800 行估 **1.3 天**
  - v0.2：实测 1977 行 → **1.4 天**（+0.1）
  - **v0.2.2**：发现 `sandboxWsRoute.ts` 重复造轮子（`engine.ts` 已做分发），取消该独立 utils，合并步骤 1+2 为"composables + engine handlers 工厂"节省 0.1 天 → **回到 1.3 天**，总节点预算 **3.3 天**

#### 13.1.2 拆分后目标结构（v0.2.3 修订）

> v0.2.3 核查结果：气泡是 Phaser GameObjects 不是 Vue DOM；SandboxEvents / SandboxReflections / SandboxTimeline / SandboxEventInjectorDialog 已在 M4.5 之前独立（不纳入本次拆分）。

| 新组件 | 职责 | 预估行数 | 独立单测 |
|---|---|---|---|
| `SandboxMap.vue` | **Phaser 场景 + NPC 精灵渲染 + 气泡层三位一体 + 点击/拖拽/右键/滚轮缩放** | ~500 | mount 快照 1（场景挂载 + 无 NPC 空态） + 气泡显隐 2 = **3** |
| `SandboxInput.vue` | 玩家输入区（文本 + 发送，未来 @mention 扩展点） | ~220 | mount 渲染 1 + emit 提交 1 = **2** |
| `SandboxGoals.vue` | NPC 目标面板（列表 + 新建 + 状态切换） | ~280 | mount 列表 1 + 创建 emit 1 = **2** |
| `SandboxControls.vue` | 引擎启停 + interval/ticks 滑块 + dry-run + 气泡开关 | ~160 | mount + props 切换 = **1** |
| `Sandbox.vue`（保留） | **仅编排**：布局、state 分发、composables 装配、WS 订阅调用 | **<300** | mount 快照 = **1** |

**已有组件（保留不动）**：
- `SandboxTimeline.vue`（M4.2.1.c）·`SandboxReflections.vue`（M4.2.3.c）·`SandboxEvents.vue`（M4.2.4.c）·`SandboxEventInjectorDialog.vue`（M4.3.x）

#### 13.1.3 配套提取（v0.2.2 修订：取消 sandboxWsRoute.ts，handler 工厂并入 composables）

> ⚠️ **三轮零改动核查结果**（2026-04-25）：
> - ❌ 项目**无 pinia 依赖**（`package.json` / `defineStore` 全局 0 匹配）—— 原"pinia 拆 2 store"规划**无法落地** → 用 composables 代替
> - ⚠️ `frontend/src/utils/sandbox.ts` **已存在**并已承载 `extractBubbleText / fallbackPosition / clamp / snapTo / CATEGORY_COLOR` —— 保留不动
> - ❌ `frontend/src/api/engine.ts::openEngineWs` **已内建 `switch(msg.type)` 分发**到 `onTickStart / onNpcUpdated / onTickEnd / onError / onMetaWarn / onReflection / onSceneEvent` 7 种 handler（L204-219）—— 原计划新增 `sandboxWsRoute.ts` 做"按 type 派发到 handler"**属于重复造轮子**，**取消**
>
> 修订方向：WS 分发层交给 `engine.ts`；Sandbox.vue 只需要"把 composable 的 apply\* 方法组装成 `EngineWsHandlers` 对象"—— 这一层**就是 composable 的 public API 的一部分**（`useSandboxSimulation().toEngineHandlers()`），不必独立成文件。

| 文件 | 职责 | 新增纯函数单测 |
|---|---|---|
| `frontend/src/utils/sandbox.ts` | **保留不动**：已有气泡 / 坐标兜底 / 分类色 / 数值工具 | 保留现有测试 |
| `frontend/src/utils/planPath.ts` | **保留不动**：已有 plan_path 徽章（M4.5.1.c） | 保留现有 13 条 |
| ~~`frontend/src/utils/sandboxWsRoute.ts`~~ | ~~WS msg 路由分发器~~ | **v0.2.2 取消**（`engine.ts` 已做） |
| `frontend/src/composables/useSandboxSimulation.ts` | **新增**：engine running / tick / 当前 NPCs 最新 meta 的 reactive 状态 + 暴露 `toEngineHandlers()` 生成 `EngineWsHandlers` | **+6**（原 3：mount/unmount / tick 增量更新 / 引擎启停；新增 3：toEngineHandlers 返回形状 / onTickEnd 触发 applyTickEnd / onNpcUpdated 触发 applyNpcUpdated） |
| `frontend/src/composables/useSandboxTimeline.ts` | **新增**：TimelineNpcEntry 列表 + 增量合并 + 最新 tick 号 + 暴露 `toTimelineHandlers()` | **+6**（原 3：合并 / 越过旧 tick / plan_path 归一；新增 3：handlers 返回形状 / onReflection 追加 / onSceneEvent 追加） |

**composable 约定**（与 pinia 等价的 API 形态，新增 `toEngineHandlers` / `toTimelineHandlers` 把 handler 工厂并进来）：
```typescript
export function useSandboxSimulation() {
  const engineRunning = ref(false)
  const latestTick = ref<number | null>(null)
  const npcsMeta = reactive(new Map<number, SimulationMetaV1>())

  function applyTickEnd(msg: WsTickEndMsg) { /* ... */ }
  function applyNpcUpdated(msg: WsTickNpcUpdatedMsg) { /* ... */ }

  /** 把 apply* 方法装配为 EngineWsHandlers，直接传给 openEngineWs */
  function toEngineHandlers(): Partial<EngineWsHandlers> {
    return {
      onTickEnd: (m) => applyTickEnd(m),
      onNpcUpdated: (m) => applyNpcUpdated(m),
    }
  }

  return { engineRunning, latestTick, npcsMeta, applyTickEnd, applyNpcUpdated, toEngineHandlers } as const
}

// Sandbox.vue 顶层组装：
const sim = useSandboxSimulation()
const tl = useSandboxTimeline()
openEngineWs(endpoint, sceneId, { ...sim.toEngineHandlers(), ...tl.toTimelineHandlers() })
```
- 单例 vs 工厂：本节点按"每次调用新建实例"（与 pinia 单例语义**不同**），Sandbox.vue 顶层调用一次后通过 props 传给子组件；未来引入 pinia 可平滑迁移（pinia store 即 "special composable with plugin"）
- handler 组合边界：simulation 只负责 tick 状态，timeline 只负责时间线数据，没有 handler 重叠；两者的 `toXxxHandlers` 对象键不相交，可安全 spread 合并。

#### 13.1.4 工作流（小步 commit，v0.2.3 修订：5 步 → 4 步）

1. `feat(M4.6.0·批次A): 新增 useSandboxSimulation + useSandboxTimeline composables + 单测` ✅ **已完成（8d030a5）**
2. `feat(M4.6.0·批次B): 抽离 SandboxMap 组件（Phaser + NPC 节点 + 气泡层三位一体）` ⏳ **进行中**
3. `feat(M4.6.0·批次C): 抽离 SandboxInput / SandboxGoals / SandboxControls 组件 + mount 测试`
4. `refactor(M4.6.0·批次C): Sandbox.vue 瘦身至 <300 行编排层 + mount 快照测试`

每步必须：`vue-tsc -b` 全绿 + `npm test` 全绿 + 手动 2D 沙盒 live smoke（气泡/徽章/timeline/目标/事件都刷新）。

按用户批次节奏（2026-04-25 Q：方案 B）：
- **批次 A**：步骤 1 composables ✅ commit `2a9dcc7 + 8d030a5`
- **批次 B**：步骤 2 SandboxMap（最硬骨头，独立 commit）⏳
- **批次 C**：步骤 3+4 其他组件 + Sandbox.vue 瘦身 → 1~2 commit

#### 13.1.5 验收（v0.2.3 修订）

- [x] 2 个 composable 就位（✅ 批次 A 完成）
- [ ] 4 个新组件（SandboxMap / SandboxInput / SandboxGoals / SandboxControls）就位
- [ ] `Sandbox.vue` **<300 行**（1977 行 → <300，瘦身 >85%）
- [ ] 前端单测 36 → **≥57**（当前 54 + 批次 B/C 新增 mount 快照 ≥3）
- [ ] `vue-tsc` + `npm test` + 手动 live smoke 三件套全绿
- [ ] 视觉回归：每个视图（气泡 / timeline / 目标面板 / 事件抽屉 / 引擎控制）与拆分前像素级一致（用户人眼比对）

### 13.2 M4.6.1 · N-1 LLM 自主 goal_done + U-D P95 观测（1.8 天）

#### 13.2.1 N-1 LLM 自主 goal_done（0.8 天）

**核心**：让 plan node 的 LLM 输出结构化 `goal_done: boolean` 字段；scheduler 消费后自动 PATCH goal status=done。

| 项 | 内容 |
|---|---|
| prompt 扩参 | `buildPlanPrompt` 当 `activeGoal` 存在时，system prompt 追加："若你认为已经**彻底完成**当前目标，请在 response 里输出 `goal_done=true`；否则不输出此字段" |
| LLM 输出解析 | `parseLlmPlanResp` 增加 `goal_done?: boolean`；解析失败 / 非 boolean 一律按 `false` 处理（容错） |
| scheduler 消费 | tick 结束后，若 `plan_path='goal'` 且 `goal_done=true`，调用 `updateGoal(id, { status: 'done' })`；打日志 `[goal.self-report] npc=X goal=Y closed on tick=T` |
| 降级 | `N1_LLM_GOAL_DONE_ENABLED=false`（新 env，默认 true）时跳过本逻辑，完全退回 M4.5 外部 PATCH 模式 |
| 单测 | +5（prompt 注入 1 / 解析容错 3 / scheduler 消费闭环 1） |
| 验收 | live smoke：下 goal → 观察到 LLM 输出 goal_done=true → goal status=done 无需人工 PATCH |

#### 13.2.2 U-D P95 histogram 小窗口（1.0 天）

**核心**：把 M4.2 起就欠的 tick P95 观测补上；小窗口、前端可见、无 Prometheus 依赖。

| 项 | 内容 |
|---|---|
| 后端 metrics | `engine/metrics/tickDuration.ts`：in-memory 环形缓冲（最近 100 次 tick duration_ms），暴露 `GET /api/engine/metrics?scene_id=X` 返回 `{ p50, p95, p99, recent: [100 个数字] }` |
| 计算 | 简单排序取百分位；不引新依赖 |
| WS 广播（可选） | `tick.end.summary` 消息附带本 tick duration_ms；前端可实时更新柱状图 |
| 前端 | `frontend/src/components/ObserveBar.vue`：Sandbox header 下方一条 24px 高观测条，显示 `P50/P95/P99` 三数字 + 微型柱状图（用 `<svg>` 手绘，不引入 chart 库） |
| 单测 | 后端 +3（环形缓冲 1 / 百分位算 1 / REST 1）；前端 +3（props 渲染 1 / 百分位展示 1 / 柱状图 0 值处理 1） |
| 验收 | live smoke 跑 20 tick → ObserveBar 持续刷新；P95 数字与手工算一致（±1ms） |

#### 13.2.3 验收

- [ ] `N1_LLM_GOAL_DONE_ENABLED=true`（默认）时，live smoke 下 goal → LLM 输出 goal_done → goal 自动 done
- [ ] `N1_LLM_GOAL_DONE_ENABLED=false` 时完全回退 M4.5 行为
- [ ] `GET /api/engine/metrics?scene_id=1` 返回合法 p50/p95/p99
- [ ] ObserveBar 在 Sandbox 可见并实时刷新
- [ ] 后端单测 261 → **≥269**（+8：5 + 3）
- [ ] 前端单测 47 → **≥50**（+3）

### 13.3 总规模预估（v0.2.2 修订）

| 指标 | 基线 | 目标 | Δ |
|---|---|---|---|
| 后端单测 | 261 | **≥269** | +8（N-1 5 / U-D 3） |
| 前端单测 | 36 | **≥57** | +21（U-C 18 实测 + mount ≥3 预留 + U-D 3） |
| 新表 | 0 | **0** | — |
| 新列 | 0 | **0** | — |
| 新 REST | 1 | `GET /api/engine/metrics`（1） | +1 |
| 新 WS 字段 | 0 | `tick.end.summary.duration_ms`（可选） | 0~1 |
| 新前端组件 | 0 | **4** 拆分（SandboxMap 含气泡 / Input / Goals / Controls）+ **1** ObserveBar = 5 | +5 |
| 新 composables | 0 | 2（useSandboxSimulation + useSandboxTimeline） | +2 |
| 新 utils | 0 | **0**（sandbox.ts / planPath.ts 保留不动；sandboxWsRoute.ts 经核查取消） | 0 |
| 新 env | 0 | 1 `N1_LLM_GOAL_DONE_ENABLED` | +1 |
| **总耗时** | — | **3.3 天**（1.3 + 0.8 + 1.0 + 0.2 buffer；v0.2.2 合并步骤 1+2 节省 0.1 天） | — |

### 13.4 整节点关盘

- [ ] M4.6.0 / M4.6.1 两子节点全绿 + live smoke 走通
- [ ] `docs/m4.6-roadmap.md` 更新到 **v1.0 CLOSED**
- [ ] `docs/m4.6-summary.md` + `docs/m4.6-showcase.md` 产出
- [ ] `git tag v0.4.6-closed` + push

---

## 14. M4.7 预留条款（U-R @mention + U-Q 对话聚合）

v0.2 评审时同时锁定 M4.7 的预设，便于 M4.6 关盘后无缝启动。

### 14.1 预设清单

| 预设 | 选择 | 理由 |
|---|---|---|
| Q2 @mention 形态 | **a 前端显式 UI**：发消息前强制选目标 NPC，`scene_event.payload.target_npc_id=X` | 与 dialogue.event visible_npcs=1 兼容；无歧义；UX 可接受（NPC 头像列表直接点击） |
| Q3 conversation 数据模型 | **b 标准**：`id/scene_id/root_event_id/participants JSON/last_turn_at/turn_count/last_content/status` | 前端列表渲染省 JOIN；后端维护代价可控；未来加 topic/avg_interval 可平滑扩列 |
| Q4 会话生命周期 | **c 自动 + 玩家手动**：基于 `CONVERSATION_IDLE_TIMEOUT_SEC` 自动 ended；玩家可点"结束会话"按钮强制归档 | 最接近产品直觉；自动兜底防遗漏；手动入口覆盖"玩家明确结束"场景 |

### 14.2 M4.7 启动时需直接照搬的 v0.1 章节

- §5 数据模型草图（`conversation` 建表 + `scene_event.conversation_id` + 历史回填 SQL）
- §6 环境变量规划（`CONVERSATION_ENABLED` / `CONVERSATION_IDLE_TIMEOUT_SEC` / `MENTION_ENABLED` / `MENTION_PROMPT_STRENGTH`）
- §7 观测埋点（`[conversation.crud] endByTimeout` / `[mention] target=X` 等）
- §8.2 R-1 ~ R-7 风险清单（M4.7 启动需消化 7 条风险）

### 14.3 M4.6 关盘后对 M4.7 的辅助

- Sandbox.vue 已深拆为 7 组件 → SandboxInput 可直接扩 @mention picker、SandboxEvents 可直接按 conversation 分组
- ObserveBar 已存在 → M4.7 可扩一行显示 conversation_active_count
- goal 自主 goal_done 已打通 → M4.7 加 conversation_done 判断可复用相同 LLM 输出范式

---

**M4.6 v0.2 定稿完成，可直接按 §13 开始实现 M4.6.0。**
