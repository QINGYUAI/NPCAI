# 后端 API 前缀
# - 空 或 "/api"：推荐。浏览器通过同源 + Vite proxy 访问（支持局域网/手机/Tailscale 直连）
# - 完整 URL（如 "http://10.0.0.2:3000/api"）：直连后端；需后端放开 CORS
VITE_API_BASE=/api

# WebSocket 前缀（可选）
# - 留空：从 VITE_API_BASE 推导；相对路径时走 window.location（同源）
# - 显式覆盖：用于反向代理或不同 host 场景，例如 "ws://10.0.0.2:3000"
# VITE_WS_BASE=
